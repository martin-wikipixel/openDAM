<?php

/**
 * Base class that represents a row from the 'folder' table.
 *
 * 
 *
 * This class was autogenerated by Propel 1.4.2 on:
 *
 * Thu Oct 31 14:46:50 2013
 *
 * @package    lib.model.om
 */
abstract class BaseFolder extends BaseObject  implements Persistent {


	/**
	 * The Peer class.
	 * Instance provides a convenient way of calling static methods on a class
	 * that calling code may not be able to identify.
	 * @var        FolderPeer
	 */
	protected static $peer;

	/**
	 * The value for the id field.
	 * @var        int
	 */
	protected $id;

	/**
	 * The value for the name field.
	 * @var        string
	 */
	protected $name;

	/**
	 * The value for the description field.
	 * @var        string
	 */
	protected $description;

	/**
	 * The value for the lat field.
	 * @var        string
	 */
	protected $lat;

	/**
	 * The value for the lng field.
	 * @var        string
	 */
	protected $lng;

	/**
	 * The value for the user_id field.
	 * @var        int
	 */
	protected $user_id;

	/**
	 * The value for the groupe_id field.
	 * @var        int
	 */
	protected $groupe_id;

	/**
	 * The value for the created_at field.
	 * @var        string
	 */
	protected $created_at;

	/**
	 * The value for the subfolder_id field.
	 * @var        int
	 */
	protected $subfolder_id;

	/**
	 * The value for the state field.
	 * Note: this column has a database default value of: 1
	 * @var        int
	 */
	protected $state;

	/**
	 * The value for the licence_id field.
	 * @var        int
	 */
	protected $licence_id;

	/**
	 * The value for the usage_distribution_id field.
	 * @var        int
	 */
	protected $usage_distribution_id;

	/**
	 * The value for the usage_constraint_id field.
	 * @var        int
	 */
	protected $usage_constraint_id;

	/**
	 * The value for the usage_use_id field.
	 * @var        int
	 */
	protected $usage_use_id;

	/**
	 * The value for the usage_commercial_id field.
	 * @var        int
	 */
	protected $usage_commercial_id;

	/**
	 * The value for the creative_commons_id field.
	 * @var        int
	 */
	protected $creative_commons_id;

	/**
	 * The value for the free field.
	 * @var        boolean
	 */
	protected $free;

	/**
	 * The value for the thumbnail field.
	 * @var        string
	 */
	protected $thumbnail;

	/**
	 * The value for the disk_id field.
	 * @var        int
	 */
	protected $disk_id;

	/**
	 * @var        User
	 */
	protected $aUser;

	/**
	 * @var        Groupe
	 */
	protected $aGroupe;

	/**
	 * @var        Folder
	 */
	protected $aFolderRelatedBySubfolderId;

	/**
	 * @var        Licence
	 */
	protected $aLicence;

	/**
	 * @var        UsageDistribution
	 */
	protected $aUsageDistribution;

	/**
	 * @var        UsageConstraint
	 */
	protected $aUsageConstraint;

	/**
	 * @var        UsageUse
	 */
	protected $aUsageUse;

	/**
	 * @var        UsageCommercial
	 */
	protected $aUsageCommercial;

	/**
	 * @var        CreativeCommons
	 */
	protected $aCreativeCommons;

	/**
	 * @var        Disk
	 */
	protected $aDisk;

	/**
	 * @var        array File[] Collection to store aggregation of File objects.
	 */
	protected $collFiles;

	/**
	 * @var        Criteria The criteria used to select the current contents of collFiles.
	 */
	private $lastFileCriteria = null;

	/**
	 * @var        array FileTmp[] Collection to store aggregation of FileTmp objects.
	 */
	protected $collFileTmps;

	/**
	 * @var        Criteria The criteria used to select the current contents of collFileTmps.
	 */
	private $lastFileTmpCriteria = null;

	/**
	 * @var        array Folder[] Collection to store aggregation of Folder objects.
	 */
	protected $collFoldersRelatedBySubfolderId;

	/**
	 * @var        Criteria The criteria used to select the current contents of collFoldersRelatedBySubfolderId.
	 */
	private $lastFolderRelatedBySubfolderIdCriteria = null;

	/**
	 * @var        array UserFolder[] Collection to store aggregation of UserFolder objects.
	 */
	protected $collUserFolders;

	/**
	 * @var        Criteria The criteria used to select the current contents of collUserFolders.
	 */
	private $lastUserFolderCriteria = null;

	/**
	 * @var        array UnitFolder[] Collection to store aggregation of UnitFolder objects.
	 */
	protected $collUnitFolders;

	/**
	 * @var        Criteria The criteria used to select the current contents of collUnitFolders.
	 */
	private $lastUnitFolderCriteria = null;

	/**
	 * Flag to prevent endless save loop, if this object is referenced
	 * by another object which falls in this transaction.
	 * @var        boolean
	 */
	protected $alreadyInSave = false;

	/**
	 * Flag to prevent endless validation loop, if this object is referenced
	 * by another object which falls in this transaction.
	 * @var        boolean
	 */
	protected $alreadyInValidation = false;

	// symfony behavior
	
	const PEER = 'FolderPeer';

	/**
	 * Applies default values to this object.
	 * This method should be called from the object's constructor (or
	 * equivalent initialization method).
	 * @see        __construct()
	 */
	public function applyDefaultValues()
	{
		$this->state = 1;
	}

	/**
	 * Initializes internal state of BaseFolder object.
	 * @see        applyDefaults()
	 */
	public function __construct()
	{
		parent::__construct();
		$this->applyDefaultValues();
	}

	/**
	 * Get the [id] column value.
	 * 
	 * @return     int
	 */
	public function getId()
	{
		return $this->id;
	}

	/**
	 * Get the [name] column value.
	 * 
	 * @return     string
	 */
	public function getName()
	{
		return $this->name;
	}

	/**
	 * Get the [description] column value.
	 * 
	 * @return     string
	 */
	public function getDescription()
	{
		return $this->description;
	}

	/**
	 * Get the [lat] column value.
	 * 
	 * @return     string
	 */
	public function getLat()
	{
		return $this->lat;
	}

	/**
	 * Get the [lng] column value.
	 * 
	 * @return     string
	 */
	public function getLng()
	{
		return $this->lng;
	}

	/**
	 * Get the [user_id] column value.
	 * 
	 * @return     int
	 */
	public function getUserId()
	{
		return $this->user_id;
	}

	/**
	 * Get the [groupe_id] column value.
	 * 
	 * @return     int
	 */
	public function getGroupeId()
	{
		return $this->groupe_id;
	}

	/**
	 * Get the [optionally formatted] temporal [created_at] column value.
	 * 
	 *
	 * @param      string $format The date/time format string (either date()-style or strftime()-style).
	 *							If format is NULL, then the raw DateTime object will be returned.
	 * @return     mixed Formatted date/time value as string or DateTime object (if format is NULL), NULL if column is NULL, and 0 if column value is 0000-00-00 00:00:00
	 * @throws     PropelException - if unable to parse/validate the date/time value.
	 */
	public function getCreatedAt($format = 'Y-m-d H:i:s')
	{
		if ($this->created_at === null) {
			return null;
		}


		if ($this->created_at === '0000-00-00 00:00:00') {
			// while technically this is not a default value of NULL,
			// this seems to be closest in meaning.
			return null;
		} else {
			try {
				$dt = new DateTime($this->created_at);
			} catch (Exception $x) {
				throw new PropelException("Internally stored date/time/timestamp value could not be converted to DateTime: " . var_export($this->created_at, true), $x);
			}
		}

		if ($format === null) {
			// Because propel.useDateTimeClass is TRUE, we return a DateTime object.
			return $dt;
		} elseif (strpos($format, '%') !== false) {
			return strftime($format, $dt->format('U'));
		} else {
			return $dt->format($format);
		}
	}

	/**
	 * Get the [subfolder_id] column value.
	 * 
	 * @return     int
	 */
	public function getSubfolderId()
	{
		return $this->subfolder_id;
	}

	/**
	 * Get the [state] column value.
	 * 
	 * @return     int
	 */
	public function getState()
	{
		return $this->state;
	}

	/**
	 * Get the [licence_id] column value.
	 * 
	 * @return     int
	 */
	public function getLicenceId()
	{
		return $this->licence_id;
	}

	/**
	 * Get the [usage_distribution_id] column value.
	 * 
	 * @return     int
	 */
	public function getUsageDistributionId()
	{
		return $this->usage_distribution_id;
	}

	/**
	 * Get the [usage_constraint_id] column value.
	 * 
	 * @return     int
	 */
	public function getUsageConstraintId()
	{
		return $this->usage_constraint_id;
	}

	/**
	 * Get the [usage_use_id] column value.
	 * 
	 * @return     int
	 */
	public function getUsageUseId()
	{
		return $this->usage_use_id;
	}

	/**
	 * Get the [usage_commercial_id] column value.
	 * 
	 * @return     int
	 */
	public function getUsageCommercialId()
	{
		return $this->usage_commercial_id;
	}

	/**
	 * Get the [creative_commons_id] column value.
	 * 
	 * @return     int
	 */
	public function getCreativeCommonsId()
	{
		return $this->creative_commons_id;
	}

	/**
	 * Get the [free] column value.
	 * 
	 * @return     boolean
	 */
	public function getFree()
	{
		return $this->free;
	}

	/**
	 * Get the [thumbnail] column value.
	 * 
	 * @return     string
	 */
	public function getThumbnail()
	{
		return $this->thumbnail;
	}

	/**
	 * Get the [disk_id] column value.
	 * 
	 * @return     int
	 */
	public function getDiskId()
	{
		return $this->disk_id;
	}

	/**
	 * Set the value of [id] column.
	 * 
	 * @param      int $v new value
	 * @return     Folder The current object (for fluent API support)
	 */
	public function setId($v)
	{
		if ($v !== null) {
			$v = (int) $v;
		}

		if ($this->id !== $v) {
			$this->id = $v;
			$this->modifiedColumns[] = FolderPeer::ID;
		}

		return $this;
	} // setId()

	/**
	 * Set the value of [name] column.
	 * 
	 * @param      string $v new value
	 * @return     Folder The current object (for fluent API support)
	 */
	public function setName($v)
	{
		if ($v !== null) {
			$v = (string) $v;
		}

		if ($this->name !== $v) {
			$this->name = $v;
			$this->modifiedColumns[] = FolderPeer::NAME;
		}

		return $this;
	} // setName()

	/**
	 * Set the value of [description] column.
	 * 
	 * @param      string $v new value
	 * @return     Folder The current object (for fluent API support)
	 */
	public function setDescription($v)
	{
		if ($v !== null) {
			$v = (string) $v;
		}

		if ($this->description !== $v) {
			$this->description = $v;
			$this->modifiedColumns[] = FolderPeer::DESCRIPTION;
		}

		return $this;
	} // setDescription()

	/**
	 * Set the value of [lat] column.
	 * 
	 * @param      string $v new value
	 * @return     Folder The current object (for fluent API support)
	 */
	public function setLat($v)
	{
		if ($v !== null) {
			$v = (string) $v;
		}

		if ($this->lat !== $v) {
			$this->lat = $v;
			$this->modifiedColumns[] = FolderPeer::LAT;
		}

		return $this;
	} // setLat()

	/**
	 * Set the value of [lng] column.
	 * 
	 * @param      string $v new value
	 * @return     Folder The current object (for fluent API support)
	 */
	public function setLng($v)
	{
		if ($v !== null) {
			$v = (string) $v;
		}

		if ($this->lng !== $v) {
			$this->lng = $v;
			$this->modifiedColumns[] = FolderPeer::LNG;
		}

		return $this;
	} // setLng()

	/**
	 * Set the value of [user_id] column.
	 * 
	 * @param      int $v new value
	 * @return     Folder The current object (for fluent API support)
	 */
	public function setUserId($v)
	{
		if ($v !== null) {
			$v = (int) $v;
		}

		if ($this->user_id !== $v) {
			$this->user_id = $v;
			$this->modifiedColumns[] = FolderPeer::USER_ID;
		}

		if ($this->aUser !== null && $this->aUser->getId() !== $v) {
			$this->aUser = null;
		}

		return $this;
	} // setUserId()

	/**
	 * Set the value of [groupe_id] column.
	 * 
	 * @param      int $v new value
	 * @return     Folder The current object (for fluent API support)
	 */
	public function setGroupeId($v)
	{
		if ($v !== null) {
			$v = (int) $v;
		}

		if ($this->groupe_id !== $v) {
			$this->groupe_id = $v;
			$this->modifiedColumns[] = FolderPeer::GROUPE_ID;
		}

		if ($this->aGroupe !== null && $this->aGroupe->getId() !== $v) {
			$this->aGroupe = null;
		}

		return $this;
	} // setGroupeId()

	/**
	 * Sets the value of [created_at] column to a normalized version of the date/time value specified.
	 * 
	 * @param      mixed $v string, integer (timestamp), or DateTime value.  Empty string will
	 *						be treated as NULL for temporal objects.
	 * @return     Folder The current object (for fluent API support)
	 */
	public function setCreatedAt($v)
	{
		// we treat '' as NULL for temporal objects because DateTime('') == DateTime('now')
		// -- which is unexpected, to say the least.
		if ($v === null || $v === '') {
			$dt = null;
		} elseif ($v instanceof DateTime) {
			$dt = $v;
		} else {
			// some string/numeric value passed; we normalize that so that we can
			// validate it.
			try {
				if (is_numeric($v)) { // if it's a unix timestamp
					$dt = new DateTime('@'.$v, new DateTimeZone('UTC'));
					// We have to explicitly specify and then change the time zone because of a
					// DateTime bug: http://bugs.php.net/bug.php?id=43003
					$dt->setTimeZone(new DateTimeZone(date_default_timezone_get()));
				} else {
					$dt = new DateTime($v);
				}
			} catch (Exception $x) {
				throw new PropelException('Error parsing date/time value: ' . var_export($v, true), $x);
			}
		}

		if ( $this->created_at !== null || $dt !== null ) {
			// (nested ifs are a little easier to read in this case)

			$currNorm = ($this->created_at !== null && $tmpDt = new DateTime($this->created_at)) ? $tmpDt->format('Y-m-d H:i:s') : null;
			$newNorm = ($dt !== null) ? $dt->format('Y-m-d H:i:s') : null;

			if ( ($currNorm !== $newNorm) // normalized values don't match 
					)
			{
				$this->created_at = ($dt ? $dt->format('Y-m-d H:i:s') : null);
				$this->modifiedColumns[] = FolderPeer::CREATED_AT;
			}
		} // if either are not null

		return $this;
	} // setCreatedAt()

	/**
	 * Set the value of [subfolder_id] column.
	 * 
	 * @param      int $v new value
	 * @return     Folder The current object (for fluent API support)
	 */
	public function setSubfolderId($v)
	{
		if ($v !== null) {
			$v = (int) $v;
		}

		if ($this->subfolder_id !== $v) {
			$this->subfolder_id = $v;
			$this->modifiedColumns[] = FolderPeer::SUBFOLDER_ID;
		}

		if ($this->aFolderRelatedBySubfolderId !== null && $this->aFolderRelatedBySubfolderId->getId() !== $v) {
			$this->aFolderRelatedBySubfolderId = null;
		}

		return $this;
	} // setSubfolderId()

	/**
	 * Set the value of [state] column.
	 * 
	 * @param      int $v new value
	 * @return     Folder The current object (for fluent API support)
	 */
	public function setState($v)
	{
		if ($v !== null) {
			$v = (int) $v;
		}

		if ($this->state !== $v || $this->isNew()) {
			$this->state = $v;
			$this->modifiedColumns[] = FolderPeer::STATE;
		}

		return $this;
	} // setState()

	/**
	 * Set the value of [licence_id] column.
	 * 
	 * @param      int $v new value
	 * @return     Folder The current object (for fluent API support)
	 */
	public function setLicenceId($v)
	{
		if ($v !== null) {
			$v = (int) $v;
		}

		if ($this->licence_id !== $v) {
			$this->licence_id = $v;
			$this->modifiedColumns[] = FolderPeer::LICENCE_ID;
		}

		if ($this->aLicence !== null && $this->aLicence->getId() !== $v) {
			$this->aLicence = null;
		}

		return $this;
	} // setLicenceId()

	/**
	 * Set the value of [usage_distribution_id] column.
	 * 
	 * @param      int $v new value
	 * @return     Folder The current object (for fluent API support)
	 */
	public function setUsageDistributionId($v)
	{
		if ($v !== null) {
			$v = (int) $v;
		}

		if ($this->usage_distribution_id !== $v) {
			$this->usage_distribution_id = $v;
			$this->modifiedColumns[] = FolderPeer::USAGE_DISTRIBUTION_ID;
		}

		if ($this->aUsageDistribution !== null && $this->aUsageDistribution->getId() !== $v) {
			$this->aUsageDistribution = null;
		}

		return $this;
	} // setUsageDistributionId()

	/**
	 * Set the value of [usage_constraint_id] column.
	 * 
	 * @param      int $v new value
	 * @return     Folder The current object (for fluent API support)
	 */
	public function setUsageConstraintId($v)
	{
		if ($v !== null) {
			$v = (int) $v;
		}

		if ($this->usage_constraint_id !== $v) {
			$this->usage_constraint_id = $v;
			$this->modifiedColumns[] = FolderPeer::USAGE_CONSTRAINT_ID;
		}

		if ($this->aUsageConstraint !== null && $this->aUsageConstraint->getId() !== $v) {
			$this->aUsageConstraint = null;
		}

		return $this;
	} // setUsageConstraintId()

	/**
	 * Set the value of [usage_use_id] column.
	 * 
	 * @param      int $v new value
	 * @return     Folder The current object (for fluent API support)
	 */
	public function setUsageUseId($v)
	{
		if ($v !== null) {
			$v = (int) $v;
		}

		if ($this->usage_use_id !== $v) {
			$this->usage_use_id = $v;
			$this->modifiedColumns[] = FolderPeer::USAGE_USE_ID;
		}

		if ($this->aUsageUse !== null && $this->aUsageUse->getId() !== $v) {
			$this->aUsageUse = null;
		}

		return $this;
	} // setUsageUseId()

	/**
	 * Set the value of [usage_commercial_id] column.
	 * 
	 * @param      int $v new value
	 * @return     Folder The current object (for fluent API support)
	 */
	public function setUsageCommercialId($v)
	{
		if ($v !== null) {
			$v = (int) $v;
		}

		if ($this->usage_commercial_id !== $v) {
			$this->usage_commercial_id = $v;
			$this->modifiedColumns[] = FolderPeer::USAGE_COMMERCIAL_ID;
		}

		if ($this->aUsageCommercial !== null && $this->aUsageCommercial->getId() !== $v) {
			$this->aUsageCommercial = null;
		}

		return $this;
	} // setUsageCommercialId()

	/**
	 * Set the value of [creative_commons_id] column.
	 * 
	 * @param      int $v new value
	 * @return     Folder The current object (for fluent API support)
	 */
	public function setCreativeCommonsId($v)
	{
		if ($v !== null) {
			$v = (int) $v;
		}

		if ($this->creative_commons_id !== $v) {
			$this->creative_commons_id = $v;
			$this->modifiedColumns[] = FolderPeer::CREATIVE_COMMONS_ID;
		}

		if ($this->aCreativeCommons !== null && $this->aCreativeCommons->getId() !== $v) {
			$this->aCreativeCommons = null;
		}

		return $this;
	} // setCreativeCommonsId()

	/**
	 * Set the value of [free] column.
	 * 
	 * @param      boolean $v new value
	 * @return     Folder The current object (for fluent API support)
	 */
	public function setFree($v)
	{
		if ($v !== null) {
			$v = (boolean) $v;
		}

		if ($this->free !== $v) {
			$this->free = $v;
			$this->modifiedColumns[] = FolderPeer::FREE;
		}

		return $this;
	} // setFree()

	/**
	 * Set the value of [thumbnail] column.
	 * 
	 * @param      string $v new value
	 * @return     Folder The current object (for fluent API support)
	 */
	public function setThumbnail($v)
	{
		if ($v !== null) {
			$v = (string) $v;
		}

		if ($this->thumbnail !== $v) {
			$this->thumbnail = $v;
			$this->modifiedColumns[] = FolderPeer::THUMBNAIL;
		}

		return $this;
	} // setThumbnail()

	/**
	 * Set the value of [disk_id] column.
	 * 
	 * @param      int $v new value
	 * @return     Folder The current object (for fluent API support)
	 */
	public function setDiskId($v)
	{
		if ($v !== null) {
			$v = (int) $v;
		}

		if ($this->disk_id !== $v) {
			$this->disk_id = $v;
			$this->modifiedColumns[] = FolderPeer::DISK_ID;
		}

		if ($this->aDisk !== null && $this->aDisk->getId() !== $v) {
			$this->aDisk = null;
		}

		return $this;
	} // setDiskId()

	/**
	 * Indicates whether the columns in this object are only set to default values.
	 *
	 * This method can be used in conjunction with isModified() to indicate whether an object is both
	 * modified _and_ has some values set which are non-default.
	 *
	 * @return     boolean Whether the columns in this object are only been set with default values.
	 */
	public function hasOnlyDefaultValues()
	{
			if ($this->state !== 1) {
				return false;
			}

		// otherwise, everything was equal, so return TRUE
		return true;
	} // hasOnlyDefaultValues()

	/**
	 * Hydrates (populates) the object variables with values from the database resultset.
	 *
	 * An offset (0-based "start column") is specified so that objects can be hydrated
	 * with a subset of the columns in the resultset rows.  This is needed, for example,
	 * for results of JOIN queries where the resultset row includes columns from two or
	 * more tables.
	 *
	 * @param      array $row The row returned by PDOStatement->fetch(PDO::FETCH_NUM)
	 * @param      int $startcol 0-based offset column which indicates which restultset column to start with.
	 * @param      boolean $rehydrate Whether this object is being re-hydrated from the database.
	 * @return     int next starting column
	 * @throws     PropelException  - Any caught Exception will be rewrapped as a PropelException.
	 */
	public function hydrate($row, $startcol = 0, $rehydrate = false)
	{
		try {

			$this->id = ($row[$startcol + 0] !== null) ? (int) $row[$startcol + 0] : null;
			$this->name = ($row[$startcol + 1] !== null) ? (string) $row[$startcol + 1] : null;
			$this->description = ($row[$startcol + 2] !== null) ? (string) $row[$startcol + 2] : null;
			$this->lat = ($row[$startcol + 3] !== null) ? (string) $row[$startcol + 3] : null;
			$this->lng = ($row[$startcol + 4] !== null) ? (string) $row[$startcol + 4] : null;
			$this->user_id = ($row[$startcol + 5] !== null) ? (int) $row[$startcol + 5] : null;
			$this->groupe_id = ($row[$startcol + 6] !== null) ? (int) $row[$startcol + 6] : null;
			$this->created_at = ($row[$startcol + 7] !== null) ? (string) $row[$startcol + 7] : null;
			$this->subfolder_id = ($row[$startcol + 8] !== null) ? (int) $row[$startcol + 8] : null;
			$this->state = ($row[$startcol + 9] !== null) ? (int) $row[$startcol + 9] : null;
			$this->licence_id = ($row[$startcol + 10] !== null) ? (int) $row[$startcol + 10] : null;
			$this->usage_distribution_id = ($row[$startcol + 11] !== null) ? (int) $row[$startcol + 11] : null;
			$this->usage_constraint_id = ($row[$startcol + 12] !== null) ? (int) $row[$startcol + 12] : null;
			$this->usage_use_id = ($row[$startcol + 13] !== null) ? (int) $row[$startcol + 13] : null;
			$this->usage_commercial_id = ($row[$startcol + 14] !== null) ? (int) $row[$startcol + 14] : null;
			$this->creative_commons_id = ($row[$startcol + 15] !== null) ? (int) $row[$startcol + 15] : null;
			$this->free = ($row[$startcol + 16] !== null) ? (boolean) $row[$startcol + 16] : null;
			$this->thumbnail = ($row[$startcol + 17] !== null) ? (string) $row[$startcol + 17] : null;
			$this->disk_id = ($row[$startcol + 18] !== null) ? (int) $row[$startcol + 18] : null;
			$this->resetModified();

			$this->setNew(false);

			if ($rehydrate) {
				$this->ensureConsistency();
			}

			// FIXME - using NUM_COLUMNS may be clearer.
			return $startcol + 19; // 19 = FolderPeer::NUM_COLUMNS - FolderPeer::NUM_LAZY_LOAD_COLUMNS).

		} catch (Exception $e) {
			throw new PropelException("Error populating Folder object", $e);
		}
	}

	/**
	 * Checks and repairs the internal consistency of the object.
	 *
	 * This method is executed after an already-instantiated object is re-hydrated
	 * from the database.  It exists to check any foreign keys to make sure that
	 * the objects related to the current object are correct based on foreign key.
	 *
	 * You can override this method in the stub class, but you should always invoke
	 * the base method from the overridden method (i.e. parent::ensureConsistency()),
	 * in case your model changes.
	 *
	 * @throws     PropelException
	 */
	public function ensureConsistency()
	{

		if ($this->aUser !== null && $this->user_id !== $this->aUser->getId()) {
			$this->aUser = null;
		}
		if ($this->aGroupe !== null && $this->groupe_id !== $this->aGroupe->getId()) {
			$this->aGroupe = null;
		}
		if ($this->aFolderRelatedBySubfolderId !== null && $this->subfolder_id !== $this->aFolderRelatedBySubfolderId->getId()) {
			$this->aFolderRelatedBySubfolderId = null;
		}
		if ($this->aLicence !== null && $this->licence_id !== $this->aLicence->getId()) {
			$this->aLicence = null;
		}
		if ($this->aUsageDistribution !== null && $this->usage_distribution_id !== $this->aUsageDistribution->getId()) {
			$this->aUsageDistribution = null;
		}
		if ($this->aUsageConstraint !== null && $this->usage_constraint_id !== $this->aUsageConstraint->getId()) {
			$this->aUsageConstraint = null;
		}
		if ($this->aUsageUse !== null && $this->usage_use_id !== $this->aUsageUse->getId()) {
			$this->aUsageUse = null;
		}
		if ($this->aUsageCommercial !== null && $this->usage_commercial_id !== $this->aUsageCommercial->getId()) {
			$this->aUsageCommercial = null;
		}
		if ($this->aCreativeCommons !== null && $this->creative_commons_id !== $this->aCreativeCommons->getId()) {
			$this->aCreativeCommons = null;
		}
		if ($this->aDisk !== null && $this->disk_id !== $this->aDisk->getId()) {
			$this->aDisk = null;
		}
	} // ensureConsistency

	/**
	 * Reloads this object from datastore based on primary key and (optionally) resets all associated objects.
	 *
	 * This will only work if the object has been saved and has a valid primary key set.
	 *
	 * @param      boolean $deep (optional) Whether to also de-associated any related objects.
	 * @param      PropelPDO $con (optional) The PropelPDO connection to use.
	 * @return     void
	 * @throws     PropelException - if this object is deleted, unsaved or doesn't have pk match in db
	 */
	public function reload($deep = false, PropelPDO $con = null)
	{
		if ($this->isDeleted()) {
			throw new PropelException("Cannot reload a deleted object.");
		}

		if ($this->isNew()) {
			throw new PropelException("Cannot reload an unsaved object.");
		}

		if ($con === null) {
			$con = Propel::getConnection(FolderPeer::DATABASE_NAME, Propel::CONNECTION_READ);
		}

		// We don't need to alter the object instance pool; we're just modifying this instance
		// already in the pool.

		$stmt = FolderPeer::doSelectStmt($this->buildPkeyCriteria(), $con);
		$row = $stmt->fetch(PDO::FETCH_NUM);
		$stmt->closeCursor();
		if (!$row) {
			throw new PropelException('Cannot find matching row in the database to reload object values.');
		}
		$this->hydrate($row, 0, true); // rehydrate

		if ($deep) {  // also de-associate any related objects?

			$this->aUser = null;
			$this->aGroupe = null;
			$this->aFolderRelatedBySubfolderId = null;
			$this->aLicence = null;
			$this->aUsageDistribution = null;
			$this->aUsageConstraint = null;
			$this->aUsageUse = null;
			$this->aUsageCommercial = null;
			$this->aCreativeCommons = null;
			$this->aDisk = null;
			$this->collFiles = null;
			$this->lastFileCriteria = null;

			$this->collFileTmps = null;
			$this->lastFileTmpCriteria = null;

			$this->collFoldersRelatedBySubfolderId = null;
			$this->lastFolderRelatedBySubfolderIdCriteria = null;

			$this->collUserFolders = null;
			$this->lastUserFolderCriteria = null;

			$this->collUnitFolders = null;
			$this->lastUnitFolderCriteria = null;

		} // if (deep)
	}

	/**
	 * Removes this object from datastore and sets delete attribute.
	 *
	 * @param      PropelPDO $con
	 * @return     void
	 * @throws     PropelException
	 * @see        BaseObject::setDeleted()
	 * @see        BaseObject::isDeleted()
	 */
	public function delete(PropelPDO $con = null)
	{
		if ($this->isDeleted()) {
			throw new PropelException("This object has already been deleted.");
		}

		if ($con === null) {
			$con = Propel::getConnection(FolderPeer::DATABASE_NAME, Propel::CONNECTION_WRITE);
		}
		
		$con->beginTransaction();
		try {
			$ret = $this->preDelete($con);
			// symfony_behaviors behavior
			foreach (sfMixer::getCallables('BaseFolder:delete:pre') as $callable)
			{
			  if (call_user_func($callable, $this, $con))
			  {
			    $con->commit();
			
			    return;
			  }
			}

			if ($ret) {
				FolderPeer::doDelete($this, $con);
				$this->postDelete($con);
				// symfony_behaviors behavior
				foreach (sfMixer::getCallables('BaseFolder:delete:post') as $callable)
				{
				  call_user_func($callable, $this, $con);
				}

				$this->setDeleted(true);
				$con->commit();
			} else {
				$con->commit();
			}
		} catch (PropelException $e) {
			$con->rollBack();
			throw $e;
		}
	}

	/**
	 * Persists this object to the database.
	 *
	 * If the object is new, it inserts it; otherwise an update is performed.
	 * All modified related objects will also be persisted in the doSave()
	 * method.  This method wraps all precipitate database operations in a
	 * single transaction.
	 *
	 * @param      PropelPDO $con
	 * @return     int The number of rows affected by this insert/update and any referring fk objects' save() operations.
	 * @throws     PropelException
	 * @see        doSave()
	 */
	public function save(PropelPDO $con = null)
	{
		if ($this->isDeleted()) {
			throw new PropelException("You cannot save an object that has been deleted.");
		}

		if ($con === null) {
			$con = Propel::getConnection(FolderPeer::DATABASE_NAME, Propel::CONNECTION_WRITE);
		}
		
		$con->beginTransaction();
		$isInsert = $this->isNew();
		try {
			$ret = $this->preSave($con);
			// symfony_behaviors behavior
			foreach (sfMixer::getCallables('BaseFolder:save:pre') as $callable)
			{
			  if (is_integer($affectedRows = call_user_func($callable, $this, $con)))
			  {
			    $con->commit();
			
			    return $affectedRows;
			  }
			}

			// symfony_timestampable behavior
			
			if ($isInsert) {
				$ret = $ret && $this->preInsert($con);
				// symfony_timestampable behavior
				if (!$this->isColumnModified(FolderPeer::CREATED_AT))
				{
				  $this->setCreatedAt(time());
				}

			} else {
				$ret = $ret && $this->preUpdate($con);
			}
			if ($ret) {
				$affectedRows = $this->doSave($con);
				if ($isInsert) {
					$this->postInsert($con);
				} else {
					$this->postUpdate($con);
				}
				$this->postSave($con);
				// symfony_behaviors behavior
				foreach (sfMixer::getCallables('BaseFolder:save:post') as $callable)
				{
				  call_user_func($callable, $this, $con, $affectedRows);
				}

				FolderPeer::addInstanceToPool($this);
			} else {
				$affectedRows = 0;
			}
			$con->commit();
			return $affectedRows;
		} catch (PropelException $e) {
			$con->rollBack();
			throw $e;
		}
	}

	/**
	 * Performs the work of inserting or updating the row in the database.
	 *
	 * If the object is new, it inserts it; otherwise an update is performed.
	 * All related objects are also updated in this method.
	 *
	 * @param      PropelPDO $con
	 * @return     int The number of rows affected by this insert/update and any referring fk objects' save() operations.
	 * @throws     PropelException
	 * @see        save()
	 */
	protected function doSave(PropelPDO $con)
	{
		$affectedRows = 0; // initialize var to track total num of affected rows
		if (!$this->alreadyInSave) {
			$this->alreadyInSave = true;

			// We call the save method on the following object(s) if they
			// were passed to this object by their coresponding set
			// method.  This object relates to these object(s) by a
			// foreign key reference.

			if ($this->aUser !== null) {
				if ($this->aUser->isModified() || $this->aUser->isNew()) {
					$affectedRows += $this->aUser->save($con);
				}
				$this->setUser($this->aUser);
			}

			if ($this->aGroupe !== null) {
				if ($this->aGroupe->isModified() || $this->aGroupe->isNew()) {
					$affectedRows += $this->aGroupe->save($con);
				}
				$this->setGroupe($this->aGroupe);
			}

			if ($this->aFolderRelatedBySubfolderId !== null) {
				if ($this->aFolderRelatedBySubfolderId->isModified() || $this->aFolderRelatedBySubfolderId->isNew()) {
					$affectedRows += $this->aFolderRelatedBySubfolderId->save($con);
				}
				$this->setFolderRelatedBySubfolderId($this->aFolderRelatedBySubfolderId);
			}

			if ($this->aLicence !== null) {
				if ($this->aLicence->isModified() || $this->aLicence->isNew()) {
					$affectedRows += $this->aLicence->save($con);
				}
				$this->setLicence($this->aLicence);
			}

			if ($this->aUsageDistribution !== null) {
				if ($this->aUsageDistribution->isModified() || $this->aUsageDistribution->isNew()) {
					$affectedRows += $this->aUsageDistribution->save($con);
				}
				$this->setUsageDistribution($this->aUsageDistribution);
			}

			if ($this->aUsageConstraint !== null) {
				if ($this->aUsageConstraint->isModified() || $this->aUsageConstraint->isNew()) {
					$affectedRows += $this->aUsageConstraint->save($con);
				}
				$this->setUsageConstraint($this->aUsageConstraint);
			}

			if ($this->aUsageUse !== null) {
				if ($this->aUsageUse->isModified() || $this->aUsageUse->isNew()) {
					$affectedRows += $this->aUsageUse->save($con);
				}
				$this->setUsageUse($this->aUsageUse);
			}

			if ($this->aUsageCommercial !== null) {
				if ($this->aUsageCommercial->isModified() || $this->aUsageCommercial->isNew()) {
					$affectedRows += $this->aUsageCommercial->save($con);
				}
				$this->setUsageCommercial($this->aUsageCommercial);
			}

			if ($this->aCreativeCommons !== null) {
				if ($this->aCreativeCommons->isModified() || $this->aCreativeCommons->isNew()) {
					$affectedRows += $this->aCreativeCommons->save($con);
				}
				$this->setCreativeCommons($this->aCreativeCommons);
			}

			if ($this->aDisk !== null) {
				if ($this->aDisk->isModified() || $this->aDisk->isNew()) {
					$affectedRows += $this->aDisk->save($con);
				}
				$this->setDisk($this->aDisk);
			}

			if ($this->isNew() ) {
				$this->modifiedColumns[] = FolderPeer::ID;
			}

			// If this object has been modified, then save it to the database.
			if ($this->isModified()) {
				if ($this->isNew()) {
					$pk = FolderPeer::doInsert($this, $con);
					$affectedRows += 1; // we are assuming that there is only 1 row per doInsert() which
										 // should always be true here (even though technically
										 // BasePeer::doInsert() can insert multiple rows).

					$this->setId($pk);  //[IMV] update autoincrement primary key

					$this->setNew(false);
				} else {
					$affectedRows += FolderPeer::doUpdate($this, $con);
				}

				$this->resetModified(); // [HL] After being saved an object is no longer 'modified'
			}

			if ($this->collFiles !== null) {
				foreach ($this->collFiles as $referrerFK) {
					if (!$referrerFK->isDeleted()) {
						$affectedRows += $referrerFK->save($con);
					}
				}
			}

			if ($this->collFileTmps !== null) {
				foreach ($this->collFileTmps as $referrerFK) {
					if (!$referrerFK->isDeleted()) {
						$affectedRows += $referrerFK->save($con);
					}
				}
			}

			if ($this->collFoldersRelatedBySubfolderId !== null) {
				foreach ($this->collFoldersRelatedBySubfolderId as $referrerFK) {
					if (!$referrerFK->isDeleted()) {
						$affectedRows += $referrerFK->save($con);
					}
				}
			}

			if ($this->collUserFolders !== null) {
				foreach ($this->collUserFolders as $referrerFK) {
					if (!$referrerFK->isDeleted()) {
						$affectedRows += $referrerFK->save($con);
					}
				}
			}

			if ($this->collUnitFolders !== null) {
				foreach ($this->collUnitFolders as $referrerFK) {
					if (!$referrerFK->isDeleted()) {
						$affectedRows += $referrerFK->save($con);
					}
				}
			}

			$this->alreadyInSave = false;

		}
		return $affectedRows;
	} // doSave()

	/**
	 * Array of ValidationFailed objects.
	 * @var        array ValidationFailed[]
	 */
	protected $validationFailures = array();

	/**
	 * Gets any ValidationFailed objects that resulted from last call to validate().
	 *
	 *
	 * @return     array ValidationFailed[]
	 * @see        validate()
	 */
	public function getValidationFailures()
	{
		return $this->validationFailures;
	}

	/**
	 * Validates the objects modified field values and all objects related to this table.
	 *
	 * If $columns is either a column name or an array of column names
	 * only those columns are validated.
	 *
	 * @param      mixed $columns Column name or an array of column names.
	 * @return     boolean Whether all columns pass validation.
	 * @see        doValidate()
	 * @see        getValidationFailures()
	 */
	public function validate($columns = null)
	{
		$res = $this->doValidate($columns);
		if ($res === true) {
			$this->validationFailures = array();
			return true;
		} else {
			$this->validationFailures = $res;
			return false;
		}
	}

	/**
	 * This function performs the validation work for complex object models.
	 *
	 * In addition to checking the current object, all related objects will
	 * also be validated.  If all pass then <code>true</code> is returned; otherwise
	 * an aggreagated array of ValidationFailed objects will be returned.
	 *
	 * @param      array $columns Array of column names to validate.
	 * @return     mixed <code>true</code> if all validations pass; array of <code>ValidationFailed</code> objets otherwise.
	 */
	protected function doValidate($columns = null)
	{
		if (!$this->alreadyInValidation) {
			$this->alreadyInValidation = true;
			$retval = null;

			$failureMap = array();


			// We call the validate method on the following object(s) if they
			// were passed to this object by their coresponding set
			// method.  This object relates to these object(s) by a
			// foreign key reference.

			if ($this->aUser !== null) {
				if (!$this->aUser->validate($columns)) {
					$failureMap = array_merge($failureMap, $this->aUser->getValidationFailures());
				}
			}

			if ($this->aGroupe !== null) {
				if (!$this->aGroupe->validate($columns)) {
					$failureMap = array_merge($failureMap, $this->aGroupe->getValidationFailures());
				}
			}

			if ($this->aFolderRelatedBySubfolderId !== null) {
				if (!$this->aFolderRelatedBySubfolderId->validate($columns)) {
					$failureMap = array_merge($failureMap, $this->aFolderRelatedBySubfolderId->getValidationFailures());
				}
			}

			if ($this->aLicence !== null) {
				if (!$this->aLicence->validate($columns)) {
					$failureMap = array_merge($failureMap, $this->aLicence->getValidationFailures());
				}
			}

			if ($this->aUsageDistribution !== null) {
				if (!$this->aUsageDistribution->validate($columns)) {
					$failureMap = array_merge($failureMap, $this->aUsageDistribution->getValidationFailures());
				}
			}

			if ($this->aUsageConstraint !== null) {
				if (!$this->aUsageConstraint->validate($columns)) {
					$failureMap = array_merge($failureMap, $this->aUsageConstraint->getValidationFailures());
				}
			}

			if ($this->aUsageUse !== null) {
				if (!$this->aUsageUse->validate($columns)) {
					$failureMap = array_merge($failureMap, $this->aUsageUse->getValidationFailures());
				}
			}

			if ($this->aUsageCommercial !== null) {
				if (!$this->aUsageCommercial->validate($columns)) {
					$failureMap = array_merge($failureMap, $this->aUsageCommercial->getValidationFailures());
				}
			}

			if ($this->aCreativeCommons !== null) {
				if (!$this->aCreativeCommons->validate($columns)) {
					$failureMap = array_merge($failureMap, $this->aCreativeCommons->getValidationFailures());
				}
			}

			if ($this->aDisk !== null) {
				if (!$this->aDisk->validate($columns)) {
					$failureMap = array_merge($failureMap, $this->aDisk->getValidationFailures());
				}
			}


			if (($retval = FolderPeer::doValidate($this, $columns)) !== true) {
				$failureMap = array_merge($failureMap, $retval);
			}


				if ($this->collFiles !== null) {
					foreach ($this->collFiles as $referrerFK) {
						if (!$referrerFK->validate($columns)) {
							$failureMap = array_merge($failureMap, $referrerFK->getValidationFailures());
						}
					}
				}

				if ($this->collFileTmps !== null) {
					foreach ($this->collFileTmps as $referrerFK) {
						if (!$referrerFK->validate($columns)) {
							$failureMap = array_merge($failureMap, $referrerFK->getValidationFailures());
						}
					}
				}

				if ($this->collFoldersRelatedBySubfolderId !== null) {
					foreach ($this->collFoldersRelatedBySubfolderId as $referrerFK) {
						if (!$referrerFK->validate($columns)) {
							$failureMap = array_merge($failureMap, $referrerFK->getValidationFailures());
						}
					}
				}

				if ($this->collUserFolders !== null) {
					foreach ($this->collUserFolders as $referrerFK) {
						if (!$referrerFK->validate($columns)) {
							$failureMap = array_merge($failureMap, $referrerFK->getValidationFailures());
						}
					}
				}

				if ($this->collUnitFolders !== null) {
					foreach ($this->collUnitFolders as $referrerFK) {
						if (!$referrerFK->validate($columns)) {
							$failureMap = array_merge($failureMap, $referrerFK->getValidationFailures());
						}
					}
				}


			$this->alreadyInValidation = false;
		}

		return (!empty($failureMap) ? $failureMap : true);
	}

	/**
	 * Retrieves a field from the object by name passed in as a string.
	 *
	 * @param      string $name name
	 * @param      string $type The type of fieldname the $name is of:
	 *                     one of the class type constants BasePeer::TYPE_PHPNAME, BasePeer::TYPE_STUDLYPHPNAME
	 *                     BasePeer::TYPE_COLNAME, BasePeer::TYPE_FIELDNAME, BasePeer::TYPE_NUM
	 * @return     mixed Value of field.
	 */
	public function getByName($name, $type = BasePeer::TYPE_PHPNAME)
	{
		$pos = FolderPeer::translateFieldName($name, $type, BasePeer::TYPE_NUM);
		$field = $this->getByPosition($pos);
		return $field;
	}

	/**
	 * Retrieves a field from the object by Position as specified in the xml schema.
	 * Zero-based.
	 *
	 * @param      int $pos position in xml schema
	 * @return     mixed Value of field at $pos
	 */
	public function getByPosition($pos)
	{
		switch($pos) {
			case 0:
				return $this->getId();
				break;
			case 1:
				return $this->getName();
				break;
			case 2:
				return $this->getDescription();
				break;
			case 3:
				return $this->getLat();
				break;
			case 4:
				return $this->getLng();
				break;
			case 5:
				return $this->getUserId();
				break;
			case 6:
				return $this->getGroupeId();
				break;
			case 7:
				return $this->getCreatedAt();
				break;
			case 8:
				return $this->getSubfolderId();
				break;
			case 9:
				return $this->getState();
				break;
			case 10:
				return $this->getLicenceId();
				break;
			case 11:
				return $this->getUsageDistributionId();
				break;
			case 12:
				return $this->getUsageConstraintId();
				break;
			case 13:
				return $this->getUsageUseId();
				break;
			case 14:
				return $this->getUsageCommercialId();
				break;
			case 15:
				return $this->getCreativeCommonsId();
				break;
			case 16:
				return $this->getFree();
				break;
			case 17:
				return $this->getThumbnail();
				break;
			case 18:
				return $this->getDiskId();
				break;
			default:
				return null;
				break;
		} // switch()
	}

	/**
	 * Exports the object as an array.
	 *
	 * You can specify the key type of the array by passing one of the class
	 * type constants.
	 *
	 * @param      string $keyType (optional) One of the class type constants BasePeer::TYPE_PHPNAME, BasePeer::TYPE_STUDLYPHPNAME
	 *                        BasePeer::TYPE_COLNAME, BasePeer::TYPE_FIELDNAME, BasePeer::TYPE_NUM. Defaults to BasePeer::TYPE_PHPNAME.
	 * @param      boolean $includeLazyLoadColumns (optional) Whether to include lazy loaded columns.  Defaults to TRUE.
	 * @return     an associative array containing the field names (as keys) and field values
	 */
	public function toArray($keyType = BasePeer::TYPE_PHPNAME, $includeLazyLoadColumns = true)
	{
		$keys = FolderPeer::getFieldNames($keyType);
		$result = array(
			$keys[0] => $this->getId(),
			$keys[1] => $this->getName(),
			$keys[2] => $this->getDescription(),
			$keys[3] => $this->getLat(),
			$keys[4] => $this->getLng(),
			$keys[5] => $this->getUserId(),
			$keys[6] => $this->getGroupeId(),
			$keys[7] => $this->getCreatedAt(),
			$keys[8] => $this->getSubfolderId(),
			$keys[9] => $this->getState(),
			$keys[10] => $this->getLicenceId(),
			$keys[11] => $this->getUsageDistributionId(),
			$keys[12] => $this->getUsageConstraintId(),
			$keys[13] => $this->getUsageUseId(),
			$keys[14] => $this->getUsageCommercialId(),
			$keys[15] => $this->getCreativeCommonsId(),
			$keys[16] => $this->getFree(),
			$keys[17] => $this->getThumbnail(),
			$keys[18] => $this->getDiskId(),
		);
		return $result;
	}

	/**
	 * Sets a field from the object by name passed in as a string.
	 *
	 * @param      string $name peer name
	 * @param      mixed $value field value
	 * @param      string $type The type of fieldname the $name is of:
	 *                     one of the class type constants BasePeer::TYPE_PHPNAME, BasePeer::TYPE_STUDLYPHPNAME
	 *                     BasePeer::TYPE_COLNAME, BasePeer::TYPE_FIELDNAME, BasePeer::TYPE_NUM
	 * @return     void
	 */
	public function setByName($name, $value, $type = BasePeer::TYPE_PHPNAME)
	{
		$pos = FolderPeer::translateFieldName($name, $type, BasePeer::TYPE_NUM);
		return $this->setByPosition($pos, $value);
	}

	/**
	 * Sets a field from the object by Position as specified in the xml schema.
	 * Zero-based.
	 *
	 * @param      int $pos position in xml schema
	 * @param      mixed $value field value
	 * @return     void
	 */
	public function setByPosition($pos, $value)
	{
		switch($pos) {
			case 0:
				$this->setId($value);
				break;
			case 1:
				$this->setName($value);
				break;
			case 2:
				$this->setDescription($value);
				break;
			case 3:
				$this->setLat($value);
				break;
			case 4:
				$this->setLng($value);
				break;
			case 5:
				$this->setUserId($value);
				break;
			case 6:
				$this->setGroupeId($value);
				break;
			case 7:
				$this->setCreatedAt($value);
				break;
			case 8:
				$this->setSubfolderId($value);
				break;
			case 9:
				$this->setState($value);
				break;
			case 10:
				$this->setLicenceId($value);
				break;
			case 11:
				$this->setUsageDistributionId($value);
				break;
			case 12:
				$this->setUsageConstraintId($value);
				break;
			case 13:
				$this->setUsageUseId($value);
				break;
			case 14:
				$this->setUsageCommercialId($value);
				break;
			case 15:
				$this->setCreativeCommonsId($value);
				break;
			case 16:
				$this->setFree($value);
				break;
			case 17:
				$this->setThumbnail($value);
				break;
			case 18:
				$this->setDiskId($value);
				break;
		} // switch()
	}

	/**
	 * Populates the object using an array.
	 *
	 * This is particularly useful when populating an object from one of the
	 * request arrays (e.g. $_POST).  This method goes through the column
	 * names, checking to see whether a matching key exists in populated
	 * array. If so the setByName() method is called for that column.
	 *
	 * You can specify the key type of the array by additionally passing one
	 * of the class type constants BasePeer::TYPE_PHPNAME, BasePeer::TYPE_STUDLYPHPNAME,
	 * BasePeer::TYPE_COLNAME, BasePeer::TYPE_FIELDNAME, BasePeer::TYPE_NUM.
	 * The default key type is the column's phpname (e.g. 'AuthorId')
	 *
	 * @param      array  $arr     An array to populate the object from.
	 * @param      string $keyType The type of keys the array uses.
	 * @return     void
	 */
	public function fromArray($arr, $keyType = BasePeer::TYPE_PHPNAME)
	{
		$keys = FolderPeer::getFieldNames($keyType);

		if (array_key_exists($keys[0], $arr)) $this->setId($arr[$keys[0]]);
		if (array_key_exists($keys[1], $arr)) $this->setName($arr[$keys[1]]);
		if (array_key_exists($keys[2], $arr)) $this->setDescription($arr[$keys[2]]);
		if (array_key_exists($keys[3], $arr)) $this->setLat($arr[$keys[3]]);
		if (array_key_exists($keys[4], $arr)) $this->setLng($arr[$keys[4]]);
		if (array_key_exists($keys[5], $arr)) $this->setUserId($arr[$keys[5]]);
		if (array_key_exists($keys[6], $arr)) $this->setGroupeId($arr[$keys[6]]);
		if (array_key_exists($keys[7], $arr)) $this->setCreatedAt($arr[$keys[7]]);
		if (array_key_exists($keys[8], $arr)) $this->setSubfolderId($arr[$keys[8]]);
		if (array_key_exists($keys[9], $arr)) $this->setState($arr[$keys[9]]);
		if (array_key_exists($keys[10], $arr)) $this->setLicenceId($arr[$keys[10]]);
		if (array_key_exists($keys[11], $arr)) $this->setUsageDistributionId($arr[$keys[11]]);
		if (array_key_exists($keys[12], $arr)) $this->setUsageConstraintId($arr[$keys[12]]);
		if (array_key_exists($keys[13], $arr)) $this->setUsageUseId($arr[$keys[13]]);
		if (array_key_exists($keys[14], $arr)) $this->setUsageCommercialId($arr[$keys[14]]);
		if (array_key_exists($keys[15], $arr)) $this->setCreativeCommonsId($arr[$keys[15]]);
		if (array_key_exists($keys[16], $arr)) $this->setFree($arr[$keys[16]]);
		if (array_key_exists($keys[17], $arr)) $this->setThumbnail($arr[$keys[17]]);
		if (array_key_exists($keys[18], $arr)) $this->setDiskId($arr[$keys[18]]);
	}

	/**
	 * Build a Criteria object containing the values of all modified columns in this object.
	 *
	 * @return     Criteria The Criteria object containing all modified values.
	 */
	public function buildCriteria()
	{
		$criteria = new Criteria(FolderPeer::DATABASE_NAME);

		if ($this->isColumnModified(FolderPeer::ID)) $criteria->add(FolderPeer::ID, $this->id);
		if ($this->isColumnModified(FolderPeer::NAME)) $criteria->add(FolderPeer::NAME, $this->name);
		if ($this->isColumnModified(FolderPeer::DESCRIPTION)) $criteria->add(FolderPeer::DESCRIPTION, $this->description);
		if ($this->isColumnModified(FolderPeer::LAT)) $criteria->add(FolderPeer::LAT, $this->lat);
		if ($this->isColumnModified(FolderPeer::LNG)) $criteria->add(FolderPeer::LNG, $this->lng);
		if ($this->isColumnModified(FolderPeer::USER_ID)) $criteria->add(FolderPeer::USER_ID, $this->user_id);
		if ($this->isColumnModified(FolderPeer::GROUPE_ID)) $criteria->add(FolderPeer::GROUPE_ID, $this->groupe_id);
		if ($this->isColumnModified(FolderPeer::CREATED_AT)) $criteria->add(FolderPeer::CREATED_AT, $this->created_at);
		if ($this->isColumnModified(FolderPeer::SUBFOLDER_ID)) $criteria->add(FolderPeer::SUBFOLDER_ID, $this->subfolder_id);
		if ($this->isColumnModified(FolderPeer::STATE)) $criteria->add(FolderPeer::STATE, $this->state);
		if ($this->isColumnModified(FolderPeer::LICENCE_ID)) $criteria->add(FolderPeer::LICENCE_ID, $this->licence_id);
		if ($this->isColumnModified(FolderPeer::USAGE_DISTRIBUTION_ID)) $criteria->add(FolderPeer::USAGE_DISTRIBUTION_ID, $this->usage_distribution_id);
		if ($this->isColumnModified(FolderPeer::USAGE_CONSTRAINT_ID)) $criteria->add(FolderPeer::USAGE_CONSTRAINT_ID, $this->usage_constraint_id);
		if ($this->isColumnModified(FolderPeer::USAGE_USE_ID)) $criteria->add(FolderPeer::USAGE_USE_ID, $this->usage_use_id);
		if ($this->isColumnModified(FolderPeer::USAGE_COMMERCIAL_ID)) $criteria->add(FolderPeer::USAGE_COMMERCIAL_ID, $this->usage_commercial_id);
		if ($this->isColumnModified(FolderPeer::CREATIVE_COMMONS_ID)) $criteria->add(FolderPeer::CREATIVE_COMMONS_ID, $this->creative_commons_id);
		if ($this->isColumnModified(FolderPeer::FREE)) $criteria->add(FolderPeer::FREE, $this->free);
		if ($this->isColumnModified(FolderPeer::THUMBNAIL)) $criteria->add(FolderPeer::THUMBNAIL, $this->thumbnail);
		if ($this->isColumnModified(FolderPeer::DISK_ID)) $criteria->add(FolderPeer::DISK_ID, $this->disk_id);

		return $criteria;
	}

	/**
	 * Builds a Criteria object containing the primary key for this object.
	 *
	 * Unlike buildCriteria() this method includes the primary key values regardless
	 * of whether or not they have been modified.
	 *
	 * @return     Criteria The Criteria object containing value(s) for primary key(s).
	 */
	public function buildPkeyCriteria()
	{
		$criteria = new Criteria(FolderPeer::DATABASE_NAME);

		$criteria->add(FolderPeer::ID, $this->id);

		return $criteria;
	}

	/**
	 * Returns the primary key for this object (row).
	 * @return     int
	 */
	public function getPrimaryKey()
	{
		return $this->getId();
	}

	/**
	 * Generic method to set the primary key (id column).
	 *
	 * @param      int $key Primary key.
	 * @return     void
	 */
	public function setPrimaryKey($key)
	{
		$this->setId($key);
	}

	/**
	 * Sets contents of passed object to values from current object.
	 *
	 * If desired, this method can also make copies of all associated (fkey referrers)
	 * objects.
	 *
	 * @param      object $copyObj An object of Folder (or compatible) type.
	 * @param      boolean $deepCopy Whether to also copy all rows that refer (by fkey) to the current row.
	 * @throws     PropelException
	 */
	public function copyInto($copyObj, $deepCopy = false)
	{

		$copyObj->setName($this->name);

		$copyObj->setDescription($this->description);

		$copyObj->setLat($this->lat);

		$copyObj->setLng($this->lng);

		$copyObj->setUserId($this->user_id);

		$copyObj->setGroupeId($this->groupe_id);

		$copyObj->setCreatedAt($this->created_at);

		$copyObj->setSubfolderId($this->subfolder_id);

		$copyObj->setState($this->state);

		$copyObj->setLicenceId($this->licence_id);

		$copyObj->setUsageDistributionId($this->usage_distribution_id);

		$copyObj->setUsageConstraintId($this->usage_constraint_id);

		$copyObj->setUsageUseId($this->usage_use_id);

		$copyObj->setUsageCommercialId($this->usage_commercial_id);

		$copyObj->setCreativeCommonsId($this->creative_commons_id);

		$copyObj->setFree($this->free);

		$copyObj->setThumbnail($this->thumbnail);

		$copyObj->setDiskId($this->disk_id);


		if ($deepCopy) {
			// important: temporarily setNew(false) because this affects the behavior of
			// the getter/setter methods for fkey referrer objects.
			$copyObj->setNew(false);

			foreach ($this->getFiles() as $relObj) {
				if ($relObj !== $this) {  // ensure that we don't try to copy a reference to ourselves
					$copyObj->addFile($relObj->copy($deepCopy));
				}
			}

			foreach ($this->getFileTmps() as $relObj) {
				if ($relObj !== $this) {  // ensure that we don't try to copy a reference to ourselves
					$copyObj->addFileTmp($relObj->copy($deepCopy));
				}
			}

			foreach ($this->getFoldersRelatedBySubfolderId() as $relObj) {
				if ($relObj !== $this) {  // ensure that we don't try to copy a reference to ourselves
					$copyObj->addFolderRelatedBySubfolderId($relObj->copy($deepCopy));
				}
			}

			foreach ($this->getUserFolders() as $relObj) {
				if ($relObj !== $this) {  // ensure that we don't try to copy a reference to ourselves
					$copyObj->addUserFolder($relObj->copy($deepCopy));
				}
			}

			foreach ($this->getUnitFolders() as $relObj) {
				if ($relObj !== $this) {  // ensure that we don't try to copy a reference to ourselves
					$copyObj->addUnitFolder($relObj->copy($deepCopy));
				}
			}

		} // if ($deepCopy)


		$copyObj->setNew(true);

		$copyObj->setId(NULL); // this is a auto-increment column, so set to default value

	}

	/**
	 * Makes a copy of this object that will be inserted as a new row in table when saved.
	 * It creates a new object filling in the simple attributes, but skipping any primary
	 * keys that are defined for the table.
	 *
	 * If desired, this method can also make copies of all associated (fkey referrers)
	 * objects.
	 *
	 * @param      boolean $deepCopy Whether to also copy all rows that refer (by fkey) to the current row.
	 * @return     Folder Clone of current object.
	 * @throws     PropelException
	 */
	public function copy($deepCopy = false)
	{
		// we use get_class(), because this might be a subclass
		$clazz = get_class($this);
		$copyObj = new $clazz();
		$this->copyInto($copyObj, $deepCopy);
		return $copyObj;
	}

	/**
	 * Returns a peer instance associated with this om.
	 *
	 * Since Peer classes are not to have any instance attributes, this method returns the
	 * same instance for all member of this class. The method could therefore
	 * be static, but this would prevent one from overriding the behavior.
	 *
	 * @return     FolderPeer
	 */
	public function getPeer()
	{
		if (self::$peer === null) {
			self::$peer = new FolderPeer();
		}
		return self::$peer;
	}

	/**
	 * Declares an association between this object and a User object.
	 *
	 * @param      User $v
	 * @return     Folder The current object (for fluent API support)
	 * @throws     PropelException
	 */
	public function setUser(User $v = null)
	{
		if ($v === null) {
			$this->setUserId(NULL);
		} else {
			$this->setUserId($v->getId());
		}

		$this->aUser = $v;

		// Add binding for other direction of this n:n relationship.
		// If this object has already been added to the User object, it will not be re-added.
		if ($v !== null) {
			$v->addFolder($this);
		}

		return $this;
	}


	/**
	 * Get the associated User object
	 *
	 * @param      PropelPDO Optional Connection object.
	 * @return     User The associated User object.
	 * @throws     PropelException
	 */
	public function getUser(PropelPDO $con = null)
	{
		if ($this->aUser === null && ($this->user_id !== null)) {
			$this->aUser = UserPeer::retrieveByPk($this->user_id);
			/* The following can be used additionally to
			   guarantee the related object contains a reference
			   to this object.  This level of coupling may, however, be
			   undesirable since it could result in an only partially populated collection
			   in the referenced object.
			   $this->aUser->addFolders($this);
			 */
		}
		return $this->aUser;
	}

	/**
	 * Declares an association between this object and a Groupe object.
	 *
	 * @param      Groupe $v
	 * @return     Folder The current object (for fluent API support)
	 * @throws     PropelException
	 */
	public function setGroupe(Groupe $v = null)
	{
		if ($v === null) {
			$this->setGroupeId(NULL);
		} else {
			$this->setGroupeId($v->getId());
		}

		$this->aGroupe = $v;

		// Add binding for other direction of this n:n relationship.
		// If this object has already been added to the Groupe object, it will not be re-added.
		if ($v !== null) {
			$v->addFolder($this);
		}

		return $this;
	}


	/**
	 * Get the associated Groupe object
	 *
	 * @param      PropelPDO Optional Connection object.
	 * @return     Groupe The associated Groupe object.
	 * @throws     PropelException
	 */
	public function getGroupe(PropelPDO $con = null)
	{
		if ($this->aGroupe === null && ($this->groupe_id !== null)) {
			$this->aGroupe = GroupePeer::retrieveByPk($this->groupe_id);
			/* The following can be used additionally to
			   guarantee the related object contains a reference
			   to this object.  This level of coupling may, however, be
			   undesirable since it could result in an only partially populated collection
			   in the referenced object.
			   $this->aGroupe->addFolders($this);
			 */
		}
		return $this->aGroupe;
	}

	/**
	 * Declares an association between this object and a Folder object.
	 *
	 * @param      Folder $v
	 * @return     Folder The current object (for fluent API support)
	 * @throws     PropelException
	 */
	public function setFolderRelatedBySubfolderId(Folder $v = null)
	{
		if ($v === null) {
			$this->setSubfolderId(NULL);
		} else {
			$this->setSubfolderId($v->getId());
		}

		$this->aFolderRelatedBySubfolderId = $v;

		// Add binding for other direction of this n:n relationship.
		// If this object has already been added to the Folder object, it will not be re-added.
		if ($v !== null) {
			$v->addFolderRelatedBySubfolderId($this);
		}

		return $this;
	}


	/**
	 * Get the associated Folder object
	 *
	 * @param      PropelPDO Optional Connection object.
	 * @return     Folder The associated Folder object.
	 * @throws     PropelException
	 */
	public function getFolderRelatedBySubfolderId(PropelPDO $con = null)
	{
		if ($this->aFolderRelatedBySubfolderId === null && ($this->subfolder_id !== null)) {
			$this->aFolderRelatedBySubfolderId = FolderPeer::retrieveByPk($this->subfolder_id);
			/* The following can be used additionally to
			   guarantee the related object contains a reference
			   to this object.  This level of coupling may, however, be
			   undesirable since it could result in an only partially populated collection
			   in the referenced object.
			   $this->aFolderRelatedBySubfolderId->addFoldersRelatedBySubfolderId($this);
			 */
		}
		return $this->aFolderRelatedBySubfolderId;
	}

	/**
	 * Declares an association between this object and a Licence object.
	 *
	 * @param      Licence $v
	 * @return     Folder The current object (for fluent API support)
	 * @throws     PropelException
	 */
	public function setLicence(Licence $v = null)
	{
		if ($v === null) {
			$this->setLicenceId(NULL);
		} else {
			$this->setLicenceId($v->getId());
		}

		$this->aLicence = $v;

		// Add binding for other direction of this n:n relationship.
		// If this object has already been added to the Licence object, it will not be re-added.
		if ($v !== null) {
			$v->addFolder($this);
		}

		return $this;
	}


	/**
	 * Get the associated Licence object
	 *
	 * @param      PropelPDO Optional Connection object.
	 * @return     Licence The associated Licence object.
	 * @throws     PropelException
	 */
	public function getLicence(PropelPDO $con = null)
	{
		if ($this->aLicence === null && ($this->licence_id !== null)) {
			$this->aLicence = LicencePeer::retrieveByPk($this->licence_id);
			/* The following can be used additionally to
			   guarantee the related object contains a reference
			   to this object.  This level of coupling may, however, be
			   undesirable since it could result in an only partially populated collection
			   in the referenced object.
			   $this->aLicence->addFolders($this);
			 */
		}
		return $this->aLicence;
	}

	/**
	 * Declares an association between this object and a UsageDistribution object.
	 *
	 * @param      UsageDistribution $v
	 * @return     Folder The current object (for fluent API support)
	 * @throws     PropelException
	 */
	public function setUsageDistribution(UsageDistribution $v = null)
	{
		if ($v === null) {
			$this->setUsageDistributionId(NULL);
		} else {
			$this->setUsageDistributionId($v->getId());
		}

		$this->aUsageDistribution = $v;

		// Add binding for other direction of this n:n relationship.
		// If this object has already been added to the UsageDistribution object, it will not be re-added.
		if ($v !== null) {
			$v->addFolder($this);
		}

		return $this;
	}


	/**
	 * Get the associated UsageDistribution object
	 *
	 * @param      PropelPDO Optional Connection object.
	 * @return     UsageDistribution The associated UsageDistribution object.
	 * @throws     PropelException
	 */
	public function getUsageDistribution(PropelPDO $con = null)
	{
		if ($this->aUsageDistribution === null && ($this->usage_distribution_id !== null)) {
			$this->aUsageDistribution = UsageDistributionPeer::retrieveByPk($this->usage_distribution_id);
			/* The following can be used additionally to
			   guarantee the related object contains a reference
			   to this object.  This level of coupling may, however, be
			   undesirable since it could result in an only partially populated collection
			   in the referenced object.
			   $this->aUsageDistribution->addFolders($this);
			 */
		}
		return $this->aUsageDistribution;
	}

	/**
	 * Declares an association between this object and a UsageConstraint object.
	 *
	 * @param      UsageConstraint $v
	 * @return     Folder The current object (for fluent API support)
	 * @throws     PropelException
	 */
	public function setUsageConstraint(UsageConstraint $v = null)
	{
		if ($v === null) {
			$this->setUsageConstraintId(NULL);
		} else {
			$this->setUsageConstraintId($v->getId());
		}

		$this->aUsageConstraint = $v;

		// Add binding for other direction of this n:n relationship.
		// If this object has already been added to the UsageConstraint object, it will not be re-added.
		if ($v !== null) {
			$v->addFolder($this);
		}

		return $this;
	}


	/**
	 * Get the associated UsageConstraint object
	 *
	 * @param      PropelPDO Optional Connection object.
	 * @return     UsageConstraint The associated UsageConstraint object.
	 * @throws     PropelException
	 */
	public function getUsageConstraint(PropelPDO $con = null)
	{
		if ($this->aUsageConstraint === null && ($this->usage_constraint_id !== null)) {
			$this->aUsageConstraint = UsageConstraintPeer::retrieveByPk($this->usage_constraint_id);
			/* The following can be used additionally to
			   guarantee the related object contains a reference
			   to this object.  This level of coupling may, however, be
			   undesirable since it could result in an only partially populated collection
			   in the referenced object.
			   $this->aUsageConstraint->addFolders($this);
			 */
		}
		return $this->aUsageConstraint;
	}

	/**
	 * Declares an association between this object and a UsageUse object.
	 *
	 * @param      UsageUse $v
	 * @return     Folder The current object (for fluent API support)
	 * @throws     PropelException
	 */
	public function setUsageUse(UsageUse $v = null)
	{
		if ($v === null) {
			$this->setUsageUseId(NULL);
		} else {
			$this->setUsageUseId($v->getId());
		}

		$this->aUsageUse = $v;

		// Add binding for other direction of this n:n relationship.
		// If this object has already been added to the UsageUse object, it will not be re-added.
		if ($v !== null) {
			$v->addFolder($this);
		}

		return $this;
	}


	/**
	 * Get the associated UsageUse object
	 *
	 * @param      PropelPDO Optional Connection object.
	 * @return     UsageUse The associated UsageUse object.
	 * @throws     PropelException
	 */
	public function getUsageUse(PropelPDO $con = null)
	{
		if ($this->aUsageUse === null && ($this->usage_use_id !== null)) {
			$this->aUsageUse = UsageUsePeer::retrieveByPk($this->usage_use_id);
			/* The following can be used additionally to
			   guarantee the related object contains a reference
			   to this object.  This level of coupling may, however, be
			   undesirable since it could result in an only partially populated collection
			   in the referenced object.
			   $this->aUsageUse->addFolders($this);
			 */
		}
		return $this->aUsageUse;
	}

	/**
	 * Declares an association between this object and a UsageCommercial object.
	 *
	 * @param      UsageCommercial $v
	 * @return     Folder The current object (for fluent API support)
	 * @throws     PropelException
	 */
	public function setUsageCommercial(UsageCommercial $v = null)
	{
		if ($v === null) {
			$this->setUsageCommercialId(NULL);
		} else {
			$this->setUsageCommercialId($v->getId());
		}

		$this->aUsageCommercial = $v;

		// Add binding for other direction of this n:n relationship.
		// If this object has already been added to the UsageCommercial object, it will not be re-added.
		if ($v !== null) {
			$v->addFolder($this);
		}

		return $this;
	}


	/**
	 * Get the associated UsageCommercial object
	 *
	 * @param      PropelPDO Optional Connection object.
	 * @return     UsageCommercial The associated UsageCommercial object.
	 * @throws     PropelException
	 */
	public function getUsageCommercial(PropelPDO $con = null)
	{
		if ($this->aUsageCommercial === null && ($this->usage_commercial_id !== null)) {
			$this->aUsageCommercial = UsageCommercialPeer::retrieveByPk($this->usage_commercial_id);
			/* The following can be used additionally to
			   guarantee the related object contains a reference
			   to this object.  This level of coupling may, however, be
			   undesirable since it could result in an only partially populated collection
			   in the referenced object.
			   $this->aUsageCommercial->addFolders($this);
			 */
		}
		return $this->aUsageCommercial;
	}

	/**
	 * Declares an association between this object and a CreativeCommons object.
	 *
	 * @param      CreativeCommons $v
	 * @return     Folder The current object (for fluent API support)
	 * @throws     PropelException
	 */
	public function setCreativeCommons(CreativeCommons $v = null)
	{
		if ($v === null) {
			$this->setCreativeCommonsId(NULL);
		} else {
			$this->setCreativeCommonsId($v->getId());
		}

		$this->aCreativeCommons = $v;

		// Add binding for other direction of this n:n relationship.
		// If this object has already been added to the CreativeCommons object, it will not be re-added.
		if ($v !== null) {
			$v->addFolder($this);
		}

		return $this;
	}


	/**
	 * Get the associated CreativeCommons object
	 *
	 * @param      PropelPDO Optional Connection object.
	 * @return     CreativeCommons The associated CreativeCommons object.
	 * @throws     PropelException
	 */
	public function getCreativeCommons(PropelPDO $con = null)
	{
		if ($this->aCreativeCommons === null && ($this->creative_commons_id !== null)) {
			$this->aCreativeCommons = CreativeCommonsPeer::retrieveByPk($this->creative_commons_id);
			/* The following can be used additionally to
			   guarantee the related object contains a reference
			   to this object.  This level of coupling may, however, be
			   undesirable since it could result in an only partially populated collection
			   in the referenced object.
			   $this->aCreativeCommons->addFolders($this);
			 */
		}
		return $this->aCreativeCommons;
	}

	/**
	 * Declares an association between this object and a Disk object.
	 *
	 * @param      Disk $v
	 * @return     Folder The current object (for fluent API support)
	 * @throws     PropelException
	 */
	public function setDisk(Disk $v = null)
	{
		if ($v === null) {
			$this->setDiskId(NULL);
		} else {
			$this->setDiskId($v->getId());
		}

		$this->aDisk = $v;

		// Add binding for other direction of this n:n relationship.
		// If this object has already been added to the Disk object, it will not be re-added.
		if ($v !== null) {
			$v->addFolder($this);
		}

		return $this;
	}


	/**
	 * Get the associated Disk object
	 *
	 * @param      PropelPDO Optional Connection object.
	 * @return     Disk The associated Disk object.
	 * @throws     PropelException
	 */
	public function getDisk(PropelPDO $con = null)
	{
		if ($this->aDisk === null && ($this->disk_id !== null)) {
			$this->aDisk = DiskPeer::retrieveByPk($this->disk_id);
			/* The following can be used additionally to
			   guarantee the related object contains a reference
			   to this object.  This level of coupling may, however, be
			   undesirable since it could result in an only partially populated collection
			   in the referenced object.
			   $this->aDisk->addFolders($this);
			 */
		}
		return $this->aDisk;
	}

	/**
	 * Clears out the collFiles collection (array).
	 *
	 * This does not modify the database; however, it will remove any associated objects, causing
	 * them to be refetched by subsequent calls to accessor method.
	 *
	 * @return     void
	 * @see        addFiles()
	 */
	public function clearFiles()
	{
		$this->collFiles = null; // important to set this to NULL since that means it is uninitialized
	}

	/**
	 * Initializes the collFiles collection (array).
	 *
	 * By default this just sets the collFiles collection to an empty array (like clearcollFiles());
	 * however, you may wish to override this method in your stub class to provide setting appropriate
	 * to your application -- for example, setting the initial array to the values stored in database.
	 *
	 * @return     void
	 */
	public function initFiles()
	{
		$this->collFiles = array();
	}

	/**
	 * Gets an array of File objects which contain a foreign key that references this object.
	 *
	 * If this collection has already been initialized with an identical Criteria, it returns the collection.
	 * Otherwise if this Folder has previously been saved, it will retrieve
	 * related Files from storage. If this Folder is new, it will return
	 * an empty collection or the current collection, the criteria is ignored on a new object.
	 *
	 * @param      PropelPDO $con
	 * @param      Criteria $criteria
	 * @return     array File[]
	 * @throws     PropelException
	 */
	public function getFiles($criteria = null, PropelPDO $con = null)
	{
		if ($criteria === null) {
			$criteria = new Criteria(FolderPeer::DATABASE_NAME);
		}
		elseif ($criteria instanceof Criteria)
		{
			$criteria = clone $criteria;
		}

		if ($this->collFiles === null) {
			if ($this->isNew()) {
			   $this->collFiles = array();
			} else {

				$criteria->add(FilePeer::FOLDER_ID, $this->id);

				FilePeer::addSelectColumns($criteria);
				$this->collFiles = FilePeer::doSelect($criteria, $con);
			}
		} else {
			// criteria has no effect for a new object
			if (!$this->isNew()) {
				// the following code is to determine if a new query is
				// called for.  If the criteria is the same as the last
				// one, just return the collection.


				$criteria->add(FilePeer::FOLDER_ID, $this->id);

				FilePeer::addSelectColumns($criteria);
				if (!isset($this->lastFileCriteria) || !$this->lastFileCriteria->equals($criteria)) {
					$this->collFiles = FilePeer::doSelect($criteria, $con);
				}
			}
		}
		$this->lastFileCriteria = $criteria;
		return $this->collFiles;
	}

	/**
	 * Returns the number of related File objects.
	 *
	 * @param      Criteria $criteria
	 * @param      boolean $distinct
	 * @param      PropelPDO $con
	 * @return     int Count of related File objects.
	 * @throws     PropelException
	 */
	public function countFiles(Criteria $criteria = null, $distinct = false, PropelPDO $con = null)
	{
		if ($criteria === null) {
			$criteria = new Criteria(FolderPeer::DATABASE_NAME);
		} else {
			$criteria = clone $criteria;
		}

		if ($distinct) {
			$criteria->setDistinct();
		}

		$count = null;

		if ($this->collFiles === null) {
			if ($this->isNew()) {
				$count = 0;
			} else {

				$criteria->add(FilePeer::FOLDER_ID, $this->id);

				$count = FilePeer::doCount($criteria, false, $con);
			}
		} else {
			// criteria has no effect for a new object
			if (!$this->isNew()) {
				// the following code is to determine if a new query is
				// called for.  If the criteria is the same as the last
				// one, just return count of the collection.


				$criteria->add(FilePeer::FOLDER_ID, $this->id);

				if (!isset($this->lastFileCriteria) || !$this->lastFileCriteria->equals($criteria)) {
					$count = FilePeer::doCount($criteria, false, $con);
				} else {
					$count = count($this->collFiles);
				}
			} else {
				$count = count($this->collFiles);
			}
		}
		return $count;
	}

	/**
	 * Method called to associate a File object to this object
	 * through the File foreign key attribute.
	 *
	 * @param      File $l File
	 * @return     void
	 * @throws     PropelException
	 */
	public function addFile(File $l)
	{
		if ($this->collFiles === null) {
			$this->initFiles();
		}
		if (!in_array($l, $this->collFiles, true)) { // only add it if the **same** object is not already associated
			array_push($this->collFiles, $l);
			$l->setFolder($this);
		}
	}


	/**
	 * If this collection has already been initialized with
	 * an identical criteria, it returns the collection.
	 * Otherwise if this Folder is new, it will return
	 * an empty collection; or if this Folder has previously
	 * been saved, it will retrieve related Files from storage.
	 *
	 * This method is protected by default in order to keep the public
	 * api reasonable.  You can provide public methods for those you
	 * actually need in Folder.
	 */
	public function getFilesJoinUser($criteria = null, $con = null, $join_behavior = Criteria::LEFT_JOIN)
	{
		if ($criteria === null) {
			$criteria = new Criteria(FolderPeer::DATABASE_NAME);
		}
		elseif ($criteria instanceof Criteria)
		{
			$criteria = clone $criteria;
		}

		if ($this->collFiles === null) {
			if ($this->isNew()) {
				$this->collFiles = array();
			} else {

				$criteria->add(FilePeer::FOLDER_ID, $this->id);

				$this->collFiles = FilePeer::doSelectJoinUser($criteria, $con, $join_behavior);
			}
		} else {
			// the following code is to determine if a new query is
			// called for.  If the criteria is the same as the last
			// one, just return the collection.

			$criteria->add(FilePeer::FOLDER_ID, $this->id);

			if (!isset($this->lastFileCriteria) || !$this->lastFileCriteria->equals($criteria)) {
				$this->collFiles = FilePeer::doSelectJoinUser($criteria, $con, $join_behavior);
			}
		}
		$this->lastFileCriteria = $criteria;

		return $this->collFiles;
	}


	/**
	 * If this collection has already been initialized with
	 * an identical criteria, it returns the collection.
	 * Otherwise if this Folder is new, it will return
	 * an empty collection; or if this Folder has previously
	 * been saved, it will retrieve related Files from storage.
	 *
	 * This method is protected by default in order to keep the public
	 * api reasonable.  You can provide public methods for those you
	 * actually need in Folder.
	 */
	public function getFilesJoinGroupe($criteria = null, $con = null, $join_behavior = Criteria::LEFT_JOIN)
	{
		if ($criteria === null) {
			$criteria = new Criteria(FolderPeer::DATABASE_NAME);
		}
		elseif ($criteria instanceof Criteria)
		{
			$criteria = clone $criteria;
		}

		if ($this->collFiles === null) {
			if ($this->isNew()) {
				$this->collFiles = array();
			} else {

				$criteria->add(FilePeer::FOLDER_ID, $this->id);

				$this->collFiles = FilePeer::doSelectJoinGroupe($criteria, $con, $join_behavior);
			}
		} else {
			// the following code is to determine if a new query is
			// called for.  If the criteria is the same as the last
			// one, just return the collection.

			$criteria->add(FilePeer::FOLDER_ID, $this->id);

			if (!isset($this->lastFileCriteria) || !$this->lastFileCriteria->equals($criteria)) {
				$this->collFiles = FilePeer::doSelectJoinGroupe($criteria, $con, $join_behavior);
			}
		}
		$this->lastFileCriteria = $criteria;

		return $this->collFiles;
	}


	/**
	 * If this collection has already been initialized with
	 * an identical criteria, it returns the collection.
	 * Otherwise if this Folder is new, it will return
	 * an empty collection; or if this Folder has previously
	 * been saved, it will retrieve related Files from storage.
	 *
	 * This method is protected by default in order to keep the public
	 * api reasonable.  You can provide public methods for those you
	 * actually need in Folder.
	 */
	public function getFilesJoinDisk($criteria = null, $con = null, $join_behavior = Criteria::LEFT_JOIN)
	{
		if ($criteria === null) {
			$criteria = new Criteria(FolderPeer::DATABASE_NAME);
		}
		elseif ($criteria instanceof Criteria)
		{
			$criteria = clone $criteria;
		}

		if ($this->collFiles === null) {
			if ($this->isNew()) {
				$this->collFiles = array();
			} else {

				$criteria->add(FilePeer::FOLDER_ID, $this->id);

				$this->collFiles = FilePeer::doSelectJoinDisk($criteria, $con, $join_behavior);
			}
		} else {
			// the following code is to determine if a new query is
			// called for.  If the criteria is the same as the last
			// one, just return the collection.

			$criteria->add(FilePeer::FOLDER_ID, $this->id);

			if (!isset($this->lastFileCriteria) || !$this->lastFileCriteria->equals($criteria)) {
				$this->collFiles = FilePeer::doSelectJoinDisk($criteria, $con, $join_behavior);
			}
		}
		$this->lastFileCriteria = $criteria;

		return $this->collFiles;
	}


	/**
	 * If this collection has already been initialized with
	 * an identical criteria, it returns the collection.
	 * Otherwise if this Folder is new, it will return
	 * an empty collection; or if this Folder has previously
	 * been saved, it will retrieve related Files from storage.
	 *
	 * This method is protected by default in order to keep the public
	 * api reasonable.  You can provide public methods for those you
	 * actually need in Folder.
	 */
	public function getFilesJoinLicence($criteria = null, $con = null, $join_behavior = Criteria::LEFT_JOIN)
	{
		if ($criteria === null) {
			$criteria = new Criteria(FolderPeer::DATABASE_NAME);
		}
		elseif ($criteria instanceof Criteria)
		{
			$criteria = clone $criteria;
		}

		if ($this->collFiles === null) {
			if ($this->isNew()) {
				$this->collFiles = array();
			} else {

				$criteria->add(FilePeer::FOLDER_ID, $this->id);

				$this->collFiles = FilePeer::doSelectJoinLicence($criteria, $con, $join_behavior);
			}
		} else {
			// the following code is to determine if a new query is
			// called for.  If the criteria is the same as the last
			// one, just return the collection.

			$criteria->add(FilePeer::FOLDER_ID, $this->id);

			if (!isset($this->lastFileCriteria) || !$this->lastFileCriteria->equals($criteria)) {
				$this->collFiles = FilePeer::doSelectJoinLicence($criteria, $con, $join_behavior);
			}
		}
		$this->lastFileCriteria = $criteria;

		return $this->collFiles;
	}


	/**
	 * If this collection has already been initialized with
	 * an identical criteria, it returns the collection.
	 * Otherwise if this Folder is new, it will return
	 * an empty collection; or if this Folder has previously
	 * been saved, it will retrieve related Files from storage.
	 *
	 * This method is protected by default in order to keep the public
	 * api reasonable.  You can provide public methods for those you
	 * actually need in Folder.
	 */
	public function getFilesJoinUsageDistribution($criteria = null, $con = null, $join_behavior = Criteria::LEFT_JOIN)
	{
		if ($criteria === null) {
			$criteria = new Criteria(FolderPeer::DATABASE_NAME);
		}
		elseif ($criteria instanceof Criteria)
		{
			$criteria = clone $criteria;
		}

		if ($this->collFiles === null) {
			if ($this->isNew()) {
				$this->collFiles = array();
			} else {

				$criteria->add(FilePeer::FOLDER_ID, $this->id);

				$this->collFiles = FilePeer::doSelectJoinUsageDistribution($criteria, $con, $join_behavior);
			}
		} else {
			// the following code is to determine if a new query is
			// called for.  If the criteria is the same as the last
			// one, just return the collection.

			$criteria->add(FilePeer::FOLDER_ID, $this->id);

			if (!isset($this->lastFileCriteria) || !$this->lastFileCriteria->equals($criteria)) {
				$this->collFiles = FilePeer::doSelectJoinUsageDistribution($criteria, $con, $join_behavior);
			}
		}
		$this->lastFileCriteria = $criteria;

		return $this->collFiles;
	}


	/**
	 * If this collection has already been initialized with
	 * an identical criteria, it returns the collection.
	 * Otherwise if this Folder is new, it will return
	 * an empty collection; or if this Folder has previously
	 * been saved, it will retrieve related Files from storage.
	 *
	 * This method is protected by default in order to keep the public
	 * api reasonable.  You can provide public methods for those you
	 * actually need in Folder.
	 */
	public function getFilesJoinUsageConstraint($criteria = null, $con = null, $join_behavior = Criteria::LEFT_JOIN)
	{
		if ($criteria === null) {
			$criteria = new Criteria(FolderPeer::DATABASE_NAME);
		}
		elseif ($criteria instanceof Criteria)
		{
			$criteria = clone $criteria;
		}

		if ($this->collFiles === null) {
			if ($this->isNew()) {
				$this->collFiles = array();
			} else {

				$criteria->add(FilePeer::FOLDER_ID, $this->id);

				$this->collFiles = FilePeer::doSelectJoinUsageConstraint($criteria, $con, $join_behavior);
			}
		} else {
			// the following code is to determine if a new query is
			// called for.  If the criteria is the same as the last
			// one, just return the collection.

			$criteria->add(FilePeer::FOLDER_ID, $this->id);

			if (!isset($this->lastFileCriteria) || !$this->lastFileCriteria->equals($criteria)) {
				$this->collFiles = FilePeer::doSelectJoinUsageConstraint($criteria, $con, $join_behavior);
			}
		}
		$this->lastFileCriteria = $criteria;

		return $this->collFiles;
	}


	/**
	 * If this collection has already been initialized with
	 * an identical criteria, it returns the collection.
	 * Otherwise if this Folder is new, it will return
	 * an empty collection; or if this Folder has previously
	 * been saved, it will retrieve related Files from storage.
	 *
	 * This method is protected by default in order to keep the public
	 * api reasonable.  You can provide public methods for those you
	 * actually need in Folder.
	 */
	public function getFilesJoinUsageUse($criteria = null, $con = null, $join_behavior = Criteria::LEFT_JOIN)
	{
		if ($criteria === null) {
			$criteria = new Criteria(FolderPeer::DATABASE_NAME);
		}
		elseif ($criteria instanceof Criteria)
		{
			$criteria = clone $criteria;
		}

		if ($this->collFiles === null) {
			if ($this->isNew()) {
				$this->collFiles = array();
			} else {

				$criteria->add(FilePeer::FOLDER_ID, $this->id);

				$this->collFiles = FilePeer::doSelectJoinUsageUse($criteria, $con, $join_behavior);
			}
		} else {
			// the following code is to determine if a new query is
			// called for.  If the criteria is the same as the last
			// one, just return the collection.

			$criteria->add(FilePeer::FOLDER_ID, $this->id);

			if (!isset($this->lastFileCriteria) || !$this->lastFileCriteria->equals($criteria)) {
				$this->collFiles = FilePeer::doSelectJoinUsageUse($criteria, $con, $join_behavior);
			}
		}
		$this->lastFileCriteria = $criteria;

		return $this->collFiles;
	}


	/**
	 * If this collection has already been initialized with
	 * an identical criteria, it returns the collection.
	 * Otherwise if this Folder is new, it will return
	 * an empty collection; or if this Folder has previously
	 * been saved, it will retrieve related Files from storage.
	 *
	 * This method is protected by default in order to keep the public
	 * api reasonable.  You can provide public methods for those you
	 * actually need in Folder.
	 */
	public function getFilesJoinUsageCommercial($criteria = null, $con = null, $join_behavior = Criteria::LEFT_JOIN)
	{
		if ($criteria === null) {
			$criteria = new Criteria(FolderPeer::DATABASE_NAME);
		}
		elseif ($criteria instanceof Criteria)
		{
			$criteria = clone $criteria;
		}

		if ($this->collFiles === null) {
			if ($this->isNew()) {
				$this->collFiles = array();
			} else {

				$criteria->add(FilePeer::FOLDER_ID, $this->id);

				$this->collFiles = FilePeer::doSelectJoinUsageCommercial($criteria, $con, $join_behavior);
			}
		} else {
			// the following code is to determine if a new query is
			// called for.  If the criteria is the same as the last
			// one, just return the collection.

			$criteria->add(FilePeer::FOLDER_ID, $this->id);

			if (!isset($this->lastFileCriteria) || !$this->lastFileCriteria->equals($criteria)) {
				$this->collFiles = FilePeer::doSelectJoinUsageCommercial($criteria, $con, $join_behavior);
			}
		}
		$this->lastFileCriteria = $criteria;

		return $this->collFiles;
	}


	/**
	 * If this collection has already been initialized with
	 * an identical criteria, it returns the collection.
	 * Otherwise if this Folder is new, it will return
	 * an empty collection; or if this Folder has previously
	 * been saved, it will retrieve related Files from storage.
	 *
	 * This method is protected by default in order to keep the public
	 * api reasonable.  You can provide public methods for those you
	 * actually need in Folder.
	 */
	public function getFilesJoinCreativeCommons($criteria = null, $con = null, $join_behavior = Criteria::LEFT_JOIN)
	{
		if ($criteria === null) {
			$criteria = new Criteria(FolderPeer::DATABASE_NAME);
		}
		elseif ($criteria instanceof Criteria)
		{
			$criteria = clone $criteria;
		}

		if ($this->collFiles === null) {
			if ($this->isNew()) {
				$this->collFiles = array();
			} else {

				$criteria->add(FilePeer::FOLDER_ID, $this->id);

				$this->collFiles = FilePeer::doSelectJoinCreativeCommons($criteria, $con, $join_behavior);
			}
		} else {
			// the following code is to determine if a new query is
			// called for.  If the criteria is the same as the last
			// one, just return the collection.

			$criteria->add(FilePeer::FOLDER_ID, $this->id);

			if (!isset($this->lastFileCriteria) || !$this->lastFileCriteria->equals($criteria)) {
				$this->collFiles = FilePeer::doSelectJoinCreativeCommons($criteria, $con, $join_behavior);
			}
		}
		$this->lastFileCriteria = $criteria;

		return $this->collFiles;
	}

	/**
	 * Clears out the collFileTmps collection (array).
	 *
	 * This does not modify the database; however, it will remove any associated objects, causing
	 * them to be refetched by subsequent calls to accessor method.
	 *
	 * @return     void
	 * @see        addFileTmps()
	 */
	public function clearFileTmps()
	{
		$this->collFileTmps = null; // important to set this to NULL since that means it is uninitialized
	}

	/**
	 * Initializes the collFileTmps collection (array).
	 *
	 * By default this just sets the collFileTmps collection to an empty array (like clearcollFileTmps());
	 * however, you may wish to override this method in your stub class to provide setting appropriate
	 * to your application -- for example, setting the initial array to the values stored in database.
	 *
	 * @return     void
	 */
	public function initFileTmps()
	{
		$this->collFileTmps = array();
	}

	/**
	 * Gets an array of FileTmp objects which contain a foreign key that references this object.
	 *
	 * If this collection has already been initialized with an identical Criteria, it returns the collection.
	 * Otherwise if this Folder has previously been saved, it will retrieve
	 * related FileTmps from storage. If this Folder is new, it will return
	 * an empty collection or the current collection, the criteria is ignored on a new object.
	 *
	 * @param      PropelPDO $con
	 * @param      Criteria $criteria
	 * @return     array FileTmp[]
	 * @throws     PropelException
	 */
	public function getFileTmps($criteria = null, PropelPDO $con = null)
	{
		if ($criteria === null) {
			$criteria = new Criteria(FolderPeer::DATABASE_NAME);
		}
		elseif ($criteria instanceof Criteria)
		{
			$criteria = clone $criteria;
		}

		if ($this->collFileTmps === null) {
			if ($this->isNew()) {
			   $this->collFileTmps = array();
			} else {

				$criteria->add(FileTmpPeer::FOLDER_ID, $this->id);

				FileTmpPeer::addSelectColumns($criteria);
				$this->collFileTmps = FileTmpPeer::doSelect($criteria, $con);
			}
		} else {
			// criteria has no effect for a new object
			if (!$this->isNew()) {
				// the following code is to determine if a new query is
				// called for.  If the criteria is the same as the last
				// one, just return the collection.


				$criteria->add(FileTmpPeer::FOLDER_ID, $this->id);

				FileTmpPeer::addSelectColumns($criteria);
				if (!isset($this->lastFileTmpCriteria) || !$this->lastFileTmpCriteria->equals($criteria)) {
					$this->collFileTmps = FileTmpPeer::doSelect($criteria, $con);
				}
			}
		}
		$this->lastFileTmpCriteria = $criteria;
		return $this->collFileTmps;
	}

	/**
	 * Returns the number of related FileTmp objects.
	 *
	 * @param      Criteria $criteria
	 * @param      boolean $distinct
	 * @param      PropelPDO $con
	 * @return     int Count of related FileTmp objects.
	 * @throws     PropelException
	 */
	public function countFileTmps(Criteria $criteria = null, $distinct = false, PropelPDO $con = null)
	{
		if ($criteria === null) {
			$criteria = new Criteria(FolderPeer::DATABASE_NAME);
		} else {
			$criteria = clone $criteria;
		}

		if ($distinct) {
			$criteria->setDistinct();
		}

		$count = null;

		if ($this->collFileTmps === null) {
			if ($this->isNew()) {
				$count = 0;
			} else {

				$criteria->add(FileTmpPeer::FOLDER_ID, $this->id);

				$count = FileTmpPeer::doCount($criteria, false, $con);
			}
		} else {
			// criteria has no effect for a new object
			if (!$this->isNew()) {
				// the following code is to determine if a new query is
				// called for.  If the criteria is the same as the last
				// one, just return count of the collection.


				$criteria->add(FileTmpPeer::FOLDER_ID, $this->id);

				if (!isset($this->lastFileTmpCriteria) || !$this->lastFileTmpCriteria->equals($criteria)) {
					$count = FileTmpPeer::doCount($criteria, false, $con);
				} else {
					$count = count($this->collFileTmps);
				}
			} else {
				$count = count($this->collFileTmps);
			}
		}
		return $count;
	}

	/**
	 * Method called to associate a FileTmp object to this object
	 * through the FileTmp foreign key attribute.
	 *
	 * @param      FileTmp $l FileTmp
	 * @return     void
	 * @throws     PropelException
	 */
	public function addFileTmp(FileTmp $l)
	{
		if ($this->collFileTmps === null) {
			$this->initFileTmps();
		}
		if (!in_array($l, $this->collFileTmps, true)) { // only add it if the **same** object is not already associated
			array_push($this->collFileTmps, $l);
			$l->setFolder($this);
		}
	}


	/**
	 * If this collection has already been initialized with
	 * an identical criteria, it returns the collection.
	 * Otherwise if this Folder is new, it will return
	 * an empty collection; or if this Folder has previously
	 * been saved, it will retrieve related FileTmps from storage.
	 *
	 * This method is protected by default in order to keep the public
	 * api reasonable.  You can provide public methods for those you
	 * actually need in Folder.
	 */
	public function getFileTmpsJoinFile($criteria = null, $con = null, $join_behavior = Criteria::LEFT_JOIN)
	{
		if ($criteria === null) {
			$criteria = new Criteria(FolderPeer::DATABASE_NAME);
		}
		elseif ($criteria instanceof Criteria)
		{
			$criteria = clone $criteria;
		}

		if ($this->collFileTmps === null) {
			if ($this->isNew()) {
				$this->collFileTmps = array();
			} else {

				$criteria->add(FileTmpPeer::FOLDER_ID, $this->id);

				$this->collFileTmps = FileTmpPeer::doSelectJoinFile($criteria, $con, $join_behavior);
			}
		} else {
			// the following code is to determine if a new query is
			// called for.  If the criteria is the same as the last
			// one, just return the collection.

			$criteria->add(FileTmpPeer::FOLDER_ID, $this->id);

			if (!isset($this->lastFileTmpCriteria) || !$this->lastFileTmpCriteria->equals($criteria)) {
				$this->collFileTmps = FileTmpPeer::doSelectJoinFile($criteria, $con, $join_behavior);
			}
		}
		$this->lastFileTmpCriteria = $criteria;

		return $this->collFileTmps;
	}


	/**
	 * If this collection has already been initialized with
	 * an identical criteria, it returns the collection.
	 * Otherwise if this Folder is new, it will return
	 * an empty collection; or if this Folder has previously
	 * been saved, it will retrieve related FileTmps from storage.
	 *
	 * This method is protected by default in order to keep the public
	 * api reasonable.  You can provide public methods for those you
	 * actually need in Folder.
	 */
	public function getFileTmpsJoinUser($criteria = null, $con = null, $join_behavior = Criteria::LEFT_JOIN)
	{
		if ($criteria === null) {
			$criteria = new Criteria(FolderPeer::DATABASE_NAME);
		}
		elseif ($criteria instanceof Criteria)
		{
			$criteria = clone $criteria;
		}

		if ($this->collFileTmps === null) {
			if ($this->isNew()) {
				$this->collFileTmps = array();
			} else {

				$criteria->add(FileTmpPeer::FOLDER_ID, $this->id);

				$this->collFileTmps = FileTmpPeer::doSelectJoinUser($criteria, $con, $join_behavior);
			}
		} else {
			// the following code is to determine if a new query is
			// called for.  If the criteria is the same as the last
			// one, just return the collection.

			$criteria->add(FileTmpPeer::FOLDER_ID, $this->id);

			if (!isset($this->lastFileTmpCriteria) || !$this->lastFileTmpCriteria->equals($criteria)) {
				$this->collFileTmps = FileTmpPeer::doSelectJoinUser($criteria, $con, $join_behavior);
			}
		}
		$this->lastFileTmpCriteria = $criteria;

		return $this->collFileTmps;
	}

	/**
	 * Clears out the collFoldersRelatedBySubfolderId collection (array).
	 *
	 * This does not modify the database; however, it will remove any associated objects, causing
	 * them to be refetched by subsequent calls to accessor method.
	 *
	 * @return     void
	 * @see        addFoldersRelatedBySubfolderId()
	 */
	public function clearFoldersRelatedBySubfolderId()
	{
		$this->collFoldersRelatedBySubfolderId = null; // important to set this to NULL since that means it is uninitialized
	}

	/**
	 * Initializes the collFoldersRelatedBySubfolderId collection (array).
	 *
	 * By default this just sets the collFoldersRelatedBySubfolderId collection to an empty array (like clearcollFoldersRelatedBySubfolderId());
	 * however, you may wish to override this method in your stub class to provide setting appropriate
	 * to your application -- for example, setting the initial array to the values stored in database.
	 *
	 * @return     void
	 */
	public function initFoldersRelatedBySubfolderId()
	{
		$this->collFoldersRelatedBySubfolderId = array();
	}

	/**
	 * Gets an array of Folder objects which contain a foreign key that references this object.
	 *
	 * If this collection has already been initialized with an identical Criteria, it returns the collection.
	 * Otherwise if this Folder has previously been saved, it will retrieve
	 * related FoldersRelatedBySubfolderId from storage. If this Folder is new, it will return
	 * an empty collection or the current collection, the criteria is ignored on a new object.
	 *
	 * @param      PropelPDO $con
	 * @param      Criteria $criteria
	 * @return     array Folder[]
	 * @throws     PropelException
	 */
	public function getFoldersRelatedBySubfolderId($criteria = null, PropelPDO $con = null)
	{
		if ($criteria === null) {
			$criteria = new Criteria(FolderPeer::DATABASE_NAME);
		}
		elseif ($criteria instanceof Criteria)
		{
			$criteria = clone $criteria;
		}

		if ($this->collFoldersRelatedBySubfolderId === null) {
			if ($this->isNew()) {
			   $this->collFoldersRelatedBySubfolderId = array();
			} else {

				$criteria->add(FolderPeer::SUBFOLDER_ID, $this->id);

				FolderPeer::addSelectColumns($criteria);
				$this->collFoldersRelatedBySubfolderId = FolderPeer::doSelect($criteria, $con);
			}
		} else {
			// criteria has no effect for a new object
			if (!$this->isNew()) {
				// the following code is to determine if a new query is
				// called for.  If the criteria is the same as the last
				// one, just return the collection.


				$criteria->add(FolderPeer::SUBFOLDER_ID, $this->id);

				FolderPeer::addSelectColumns($criteria);
				if (!isset($this->lastFolderRelatedBySubfolderIdCriteria) || !$this->lastFolderRelatedBySubfolderIdCriteria->equals($criteria)) {
					$this->collFoldersRelatedBySubfolderId = FolderPeer::doSelect($criteria, $con);
				}
			}
		}
		$this->lastFolderRelatedBySubfolderIdCriteria = $criteria;
		return $this->collFoldersRelatedBySubfolderId;
	}

	/**
	 * Returns the number of related Folder objects.
	 *
	 * @param      Criteria $criteria
	 * @param      boolean $distinct
	 * @param      PropelPDO $con
	 * @return     int Count of related Folder objects.
	 * @throws     PropelException
	 */
	public function countFoldersRelatedBySubfolderId(Criteria $criteria = null, $distinct = false, PropelPDO $con = null)
	{
		if ($criteria === null) {
			$criteria = new Criteria(FolderPeer::DATABASE_NAME);
		} else {
			$criteria = clone $criteria;
		}

		if ($distinct) {
			$criteria->setDistinct();
		}

		$count = null;

		if ($this->collFoldersRelatedBySubfolderId === null) {
			if ($this->isNew()) {
				$count = 0;
			} else {

				$criteria->add(FolderPeer::SUBFOLDER_ID, $this->id);

				$count = FolderPeer::doCount($criteria, false, $con);
			}
		} else {
			// criteria has no effect for a new object
			if (!$this->isNew()) {
				// the following code is to determine if a new query is
				// called for.  If the criteria is the same as the last
				// one, just return count of the collection.


				$criteria->add(FolderPeer::SUBFOLDER_ID, $this->id);

				if (!isset($this->lastFolderRelatedBySubfolderIdCriteria) || !$this->lastFolderRelatedBySubfolderIdCriteria->equals($criteria)) {
					$count = FolderPeer::doCount($criteria, false, $con);
				} else {
					$count = count($this->collFoldersRelatedBySubfolderId);
				}
			} else {
				$count = count($this->collFoldersRelatedBySubfolderId);
			}
		}
		return $count;
	}

	/**
	 * Method called to associate a Folder object to this object
	 * through the Folder foreign key attribute.
	 *
	 * @param      Folder $l Folder
	 * @return     void
	 * @throws     PropelException
	 */
	public function addFolderRelatedBySubfolderId(Folder $l)
	{
		if ($this->collFoldersRelatedBySubfolderId === null) {
			$this->initFoldersRelatedBySubfolderId();
		}
		if (!in_array($l, $this->collFoldersRelatedBySubfolderId, true)) { // only add it if the **same** object is not already associated
			array_push($this->collFoldersRelatedBySubfolderId, $l);
			$l->setFolderRelatedBySubfolderId($this);
		}
	}


	/**
	 * If this collection has already been initialized with
	 * an identical criteria, it returns the collection.
	 * Otherwise if this Folder is new, it will return
	 * an empty collection; or if this Folder has previously
	 * been saved, it will retrieve related FoldersRelatedBySubfolderId from storage.
	 *
	 * This method is protected by default in order to keep the public
	 * api reasonable.  You can provide public methods for those you
	 * actually need in Folder.
	 */
	public function getFoldersRelatedBySubfolderIdJoinUser($criteria = null, $con = null, $join_behavior = Criteria::LEFT_JOIN)
	{
		if ($criteria === null) {
			$criteria = new Criteria(FolderPeer::DATABASE_NAME);
		}
		elseif ($criteria instanceof Criteria)
		{
			$criteria = clone $criteria;
		}

		if ($this->collFoldersRelatedBySubfolderId === null) {
			if ($this->isNew()) {
				$this->collFoldersRelatedBySubfolderId = array();
			} else {

				$criteria->add(FolderPeer::SUBFOLDER_ID, $this->id);

				$this->collFoldersRelatedBySubfolderId = FolderPeer::doSelectJoinUser($criteria, $con, $join_behavior);
			}
		} else {
			// the following code is to determine if a new query is
			// called for.  If the criteria is the same as the last
			// one, just return the collection.

			$criteria->add(FolderPeer::SUBFOLDER_ID, $this->id);

			if (!isset($this->lastFolderRelatedBySubfolderIdCriteria) || !$this->lastFolderRelatedBySubfolderIdCriteria->equals($criteria)) {
				$this->collFoldersRelatedBySubfolderId = FolderPeer::doSelectJoinUser($criteria, $con, $join_behavior);
			}
		}
		$this->lastFolderRelatedBySubfolderIdCriteria = $criteria;

		return $this->collFoldersRelatedBySubfolderId;
	}


	/**
	 * If this collection has already been initialized with
	 * an identical criteria, it returns the collection.
	 * Otherwise if this Folder is new, it will return
	 * an empty collection; or if this Folder has previously
	 * been saved, it will retrieve related FoldersRelatedBySubfolderId from storage.
	 *
	 * This method is protected by default in order to keep the public
	 * api reasonable.  You can provide public methods for those you
	 * actually need in Folder.
	 */
	public function getFoldersRelatedBySubfolderIdJoinGroupe($criteria = null, $con = null, $join_behavior = Criteria::LEFT_JOIN)
	{
		if ($criteria === null) {
			$criteria = new Criteria(FolderPeer::DATABASE_NAME);
		}
		elseif ($criteria instanceof Criteria)
		{
			$criteria = clone $criteria;
		}

		if ($this->collFoldersRelatedBySubfolderId === null) {
			if ($this->isNew()) {
				$this->collFoldersRelatedBySubfolderId = array();
			} else {

				$criteria->add(FolderPeer::SUBFOLDER_ID, $this->id);

				$this->collFoldersRelatedBySubfolderId = FolderPeer::doSelectJoinGroupe($criteria, $con, $join_behavior);
			}
		} else {
			// the following code is to determine if a new query is
			// called for.  If the criteria is the same as the last
			// one, just return the collection.

			$criteria->add(FolderPeer::SUBFOLDER_ID, $this->id);

			if (!isset($this->lastFolderRelatedBySubfolderIdCriteria) || !$this->lastFolderRelatedBySubfolderIdCriteria->equals($criteria)) {
				$this->collFoldersRelatedBySubfolderId = FolderPeer::doSelectJoinGroupe($criteria, $con, $join_behavior);
			}
		}
		$this->lastFolderRelatedBySubfolderIdCriteria = $criteria;

		return $this->collFoldersRelatedBySubfolderId;
	}


	/**
	 * If this collection has already been initialized with
	 * an identical criteria, it returns the collection.
	 * Otherwise if this Folder is new, it will return
	 * an empty collection; or if this Folder has previously
	 * been saved, it will retrieve related FoldersRelatedBySubfolderId from storage.
	 *
	 * This method is protected by default in order to keep the public
	 * api reasonable.  You can provide public methods for those you
	 * actually need in Folder.
	 */
	public function getFoldersRelatedBySubfolderIdJoinLicence($criteria = null, $con = null, $join_behavior = Criteria::LEFT_JOIN)
	{
		if ($criteria === null) {
			$criteria = new Criteria(FolderPeer::DATABASE_NAME);
		}
		elseif ($criteria instanceof Criteria)
		{
			$criteria = clone $criteria;
		}

		if ($this->collFoldersRelatedBySubfolderId === null) {
			if ($this->isNew()) {
				$this->collFoldersRelatedBySubfolderId = array();
			} else {

				$criteria->add(FolderPeer::SUBFOLDER_ID, $this->id);

				$this->collFoldersRelatedBySubfolderId = FolderPeer::doSelectJoinLicence($criteria, $con, $join_behavior);
			}
		} else {
			// the following code is to determine if a new query is
			// called for.  If the criteria is the same as the last
			// one, just return the collection.

			$criteria->add(FolderPeer::SUBFOLDER_ID, $this->id);

			if (!isset($this->lastFolderRelatedBySubfolderIdCriteria) || !$this->lastFolderRelatedBySubfolderIdCriteria->equals($criteria)) {
				$this->collFoldersRelatedBySubfolderId = FolderPeer::doSelectJoinLicence($criteria, $con, $join_behavior);
			}
		}
		$this->lastFolderRelatedBySubfolderIdCriteria = $criteria;

		return $this->collFoldersRelatedBySubfolderId;
	}


	/**
	 * If this collection has already been initialized with
	 * an identical criteria, it returns the collection.
	 * Otherwise if this Folder is new, it will return
	 * an empty collection; or if this Folder has previously
	 * been saved, it will retrieve related FoldersRelatedBySubfolderId from storage.
	 *
	 * This method is protected by default in order to keep the public
	 * api reasonable.  You can provide public methods for those you
	 * actually need in Folder.
	 */
	public function getFoldersRelatedBySubfolderIdJoinUsageDistribution($criteria = null, $con = null, $join_behavior = Criteria::LEFT_JOIN)
	{
		if ($criteria === null) {
			$criteria = new Criteria(FolderPeer::DATABASE_NAME);
		}
		elseif ($criteria instanceof Criteria)
		{
			$criteria = clone $criteria;
		}

		if ($this->collFoldersRelatedBySubfolderId === null) {
			if ($this->isNew()) {
				$this->collFoldersRelatedBySubfolderId = array();
			} else {

				$criteria->add(FolderPeer::SUBFOLDER_ID, $this->id);

				$this->collFoldersRelatedBySubfolderId = FolderPeer::doSelectJoinUsageDistribution($criteria, $con, $join_behavior);
			}
		} else {
			// the following code is to determine if a new query is
			// called for.  If the criteria is the same as the last
			// one, just return the collection.

			$criteria->add(FolderPeer::SUBFOLDER_ID, $this->id);

			if (!isset($this->lastFolderRelatedBySubfolderIdCriteria) || !$this->lastFolderRelatedBySubfolderIdCriteria->equals($criteria)) {
				$this->collFoldersRelatedBySubfolderId = FolderPeer::doSelectJoinUsageDistribution($criteria, $con, $join_behavior);
			}
		}
		$this->lastFolderRelatedBySubfolderIdCriteria = $criteria;

		return $this->collFoldersRelatedBySubfolderId;
	}


	/**
	 * If this collection has already been initialized with
	 * an identical criteria, it returns the collection.
	 * Otherwise if this Folder is new, it will return
	 * an empty collection; or if this Folder has previously
	 * been saved, it will retrieve related FoldersRelatedBySubfolderId from storage.
	 *
	 * This method is protected by default in order to keep the public
	 * api reasonable.  You can provide public methods for those you
	 * actually need in Folder.
	 */
	public function getFoldersRelatedBySubfolderIdJoinUsageConstraint($criteria = null, $con = null, $join_behavior = Criteria::LEFT_JOIN)
	{
		if ($criteria === null) {
			$criteria = new Criteria(FolderPeer::DATABASE_NAME);
		}
		elseif ($criteria instanceof Criteria)
		{
			$criteria = clone $criteria;
		}

		if ($this->collFoldersRelatedBySubfolderId === null) {
			if ($this->isNew()) {
				$this->collFoldersRelatedBySubfolderId = array();
			} else {

				$criteria->add(FolderPeer::SUBFOLDER_ID, $this->id);

				$this->collFoldersRelatedBySubfolderId = FolderPeer::doSelectJoinUsageConstraint($criteria, $con, $join_behavior);
			}
		} else {
			// the following code is to determine if a new query is
			// called for.  If the criteria is the same as the last
			// one, just return the collection.

			$criteria->add(FolderPeer::SUBFOLDER_ID, $this->id);

			if (!isset($this->lastFolderRelatedBySubfolderIdCriteria) || !$this->lastFolderRelatedBySubfolderIdCriteria->equals($criteria)) {
				$this->collFoldersRelatedBySubfolderId = FolderPeer::doSelectJoinUsageConstraint($criteria, $con, $join_behavior);
			}
		}
		$this->lastFolderRelatedBySubfolderIdCriteria = $criteria;

		return $this->collFoldersRelatedBySubfolderId;
	}


	/**
	 * If this collection has already been initialized with
	 * an identical criteria, it returns the collection.
	 * Otherwise if this Folder is new, it will return
	 * an empty collection; or if this Folder has previously
	 * been saved, it will retrieve related FoldersRelatedBySubfolderId from storage.
	 *
	 * This method is protected by default in order to keep the public
	 * api reasonable.  You can provide public methods for those you
	 * actually need in Folder.
	 */
	public function getFoldersRelatedBySubfolderIdJoinUsageUse($criteria = null, $con = null, $join_behavior = Criteria::LEFT_JOIN)
	{
		if ($criteria === null) {
			$criteria = new Criteria(FolderPeer::DATABASE_NAME);
		}
		elseif ($criteria instanceof Criteria)
		{
			$criteria = clone $criteria;
		}

		if ($this->collFoldersRelatedBySubfolderId === null) {
			if ($this->isNew()) {
				$this->collFoldersRelatedBySubfolderId = array();
			} else {

				$criteria->add(FolderPeer::SUBFOLDER_ID, $this->id);

				$this->collFoldersRelatedBySubfolderId = FolderPeer::doSelectJoinUsageUse($criteria, $con, $join_behavior);
			}
		} else {
			// the following code is to determine if a new query is
			// called for.  If the criteria is the same as the last
			// one, just return the collection.

			$criteria->add(FolderPeer::SUBFOLDER_ID, $this->id);

			if (!isset($this->lastFolderRelatedBySubfolderIdCriteria) || !$this->lastFolderRelatedBySubfolderIdCriteria->equals($criteria)) {
				$this->collFoldersRelatedBySubfolderId = FolderPeer::doSelectJoinUsageUse($criteria, $con, $join_behavior);
			}
		}
		$this->lastFolderRelatedBySubfolderIdCriteria = $criteria;

		return $this->collFoldersRelatedBySubfolderId;
	}


	/**
	 * If this collection has already been initialized with
	 * an identical criteria, it returns the collection.
	 * Otherwise if this Folder is new, it will return
	 * an empty collection; or if this Folder has previously
	 * been saved, it will retrieve related FoldersRelatedBySubfolderId from storage.
	 *
	 * This method is protected by default in order to keep the public
	 * api reasonable.  You can provide public methods for those you
	 * actually need in Folder.
	 */
	public function getFoldersRelatedBySubfolderIdJoinUsageCommercial($criteria = null, $con = null, $join_behavior = Criteria::LEFT_JOIN)
	{
		if ($criteria === null) {
			$criteria = new Criteria(FolderPeer::DATABASE_NAME);
		}
		elseif ($criteria instanceof Criteria)
		{
			$criteria = clone $criteria;
		}

		if ($this->collFoldersRelatedBySubfolderId === null) {
			if ($this->isNew()) {
				$this->collFoldersRelatedBySubfolderId = array();
			} else {

				$criteria->add(FolderPeer::SUBFOLDER_ID, $this->id);

				$this->collFoldersRelatedBySubfolderId = FolderPeer::doSelectJoinUsageCommercial($criteria, $con, $join_behavior);
			}
		} else {
			// the following code is to determine if a new query is
			// called for.  If the criteria is the same as the last
			// one, just return the collection.

			$criteria->add(FolderPeer::SUBFOLDER_ID, $this->id);

			if (!isset($this->lastFolderRelatedBySubfolderIdCriteria) || !$this->lastFolderRelatedBySubfolderIdCriteria->equals($criteria)) {
				$this->collFoldersRelatedBySubfolderId = FolderPeer::doSelectJoinUsageCommercial($criteria, $con, $join_behavior);
			}
		}
		$this->lastFolderRelatedBySubfolderIdCriteria = $criteria;

		return $this->collFoldersRelatedBySubfolderId;
	}


	/**
	 * If this collection has already been initialized with
	 * an identical criteria, it returns the collection.
	 * Otherwise if this Folder is new, it will return
	 * an empty collection; or if this Folder has previously
	 * been saved, it will retrieve related FoldersRelatedBySubfolderId from storage.
	 *
	 * This method is protected by default in order to keep the public
	 * api reasonable.  You can provide public methods for those you
	 * actually need in Folder.
	 */
	public function getFoldersRelatedBySubfolderIdJoinCreativeCommons($criteria = null, $con = null, $join_behavior = Criteria::LEFT_JOIN)
	{
		if ($criteria === null) {
			$criteria = new Criteria(FolderPeer::DATABASE_NAME);
		}
		elseif ($criteria instanceof Criteria)
		{
			$criteria = clone $criteria;
		}

		if ($this->collFoldersRelatedBySubfolderId === null) {
			if ($this->isNew()) {
				$this->collFoldersRelatedBySubfolderId = array();
			} else {

				$criteria->add(FolderPeer::SUBFOLDER_ID, $this->id);

				$this->collFoldersRelatedBySubfolderId = FolderPeer::doSelectJoinCreativeCommons($criteria, $con, $join_behavior);
			}
		} else {
			// the following code is to determine if a new query is
			// called for.  If the criteria is the same as the last
			// one, just return the collection.

			$criteria->add(FolderPeer::SUBFOLDER_ID, $this->id);

			if (!isset($this->lastFolderRelatedBySubfolderIdCriteria) || !$this->lastFolderRelatedBySubfolderIdCriteria->equals($criteria)) {
				$this->collFoldersRelatedBySubfolderId = FolderPeer::doSelectJoinCreativeCommons($criteria, $con, $join_behavior);
			}
		}
		$this->lastFolderRelatedBySubfolderIdCriteria = $criteria;

		return $this->collFoldersRelatedBySubfolderId;
	}


	/**
	 * If this collection has already been initialized with
	 * an identical criteria, it returns the collection.
	 * Otherwise if this Folder is new, it will return
	 * an empty collection; or if this Folder has previously
	 * been saved, it will retrieve related FoldersRelatedBySubfolderId from storage.
	 *
	 * This method is protected by default in order to keep the public
	 * api reasonable.  You can provide public methods for those you
	 * actually need in Folder.
	 */
	public function getFoldersRelatedBySubfolderIdJoinDisk($criteria = null, $con = null, $join_behavior = Criteria::LEFT_JOIN)
	{
		if ($criteria === null) {
			$criteria = new Criteria(FolderPeer::DATABASE_NAME);
		}
		elseif ($criteria instanceof Criteria)
		{
			$criteria = clone $criteria;
		}

		if ($this->collFoldersRelatedBySubfolderId === null) {
			if ($this->isNew()) {
				$this->collFoldersRelatedBySubfolderId = array();
			} else {

				$criteria->add(FolderPeer::SUBFOLDER_ID, $this->id);

				$this->collFoldersRelatedBySubfolderId = FolderPeer::doSelectJoinDisk($criteria, $con, $join_behavior);
			}
		} else {
			// the following code is to determine if a new query is
			// called for.  If the criteria is the same as the last
			// one, just return the collection.

			$criteria->add(FolderPeer::SUBFOLDER_ID, $this->id);

			if (!isset($this->lastFolderRelatedBySubfolderIdCriteria) || !$this->lastFolderRelatedBySubfolderIdCriteria->equals($criteria)) {
				$this->collFoldersRelatedBySubfolderId = FolderPeer::doSelectJoinDisk($criteria, $con, $join_behavior);
			}
		}
		$this->lastFolderRelatedBySubfolderIdCriteria = $criteria;

		return $this->collFoldersRelatedBySubfolderId;
	}

	/**
	 * Clears out the collUserFolders collection (array).
	 *
	 * This does not modify the database; however, it will remove any associated objects, causing
	 * them to be refetched by subsequent calls to accessor method.
	 *
	 * @return     void
	 * @see        addUserFolders()
	 */
	public function clearUserFolders()
	{
		$this->collUserFolders = null; // important to set this to NULL since that means it is uninitialized
	}

	/**
	 * Initializes the collUserFolders collection (array).
	 *
	 * By default this just sets the collUserFolders collection to an empty array (like clearcollUserFolders());
	 * however, you may wish to override this method in your stub class to provide setting appropriate
	 * to your application -- for example, setting the initial array to the values stored in database.
	 *
	 * @return     void
	 */
	public function initUserFolders()
	{
		$this->collUserFolders = array();
	}

	/**
	 * Gets an array of UserFolder objects which contain a foreign key that references this object.
	 *
	 * If this collection has already been initialized with an identical Criteria, it returns the collection.
	 * Otherwise if this Folder has previously been saved, it will retrieve
	 * related UserFolders from storage. If this Folder is new, it will return
	 * an empty collection or the current collection, the criteria is ignored on a new object.
	 *
	 * @param      PropelPDO $con
	 * @param      Criteria $criteria
	 * @return     array UserFolder[]
	 * @throws     PropelException
	 */
	public function getUserFolders($criteria = null, PropelPDO $con = null)
	{
		if ($criteria === null) {
			$criteria = new Criteria(FolderPeer::DATABASE_NAME);
		}
		elseif ($criteria instanceof Criteria)
		{
			$criteria = clone $criteria;
		}

		if ($this->collUserFolders === null) {
			if ($this->isNew()) {
			   $this->collUserFolders = array();
			} else {

				$criteria->add(UserFolderPeer::FOLDER_ID, $this->id);

				UserFolderPeer::addSelectColumns($criteria);
				$this->collUserFolders = UserFolderPeer::doSelect($criteria, $con);
			}
		} else {
			// criteria has no effect for a new object
			if (!$this->isNew()) {
				// the following code is to determine if a new query is
				// called for.  If the criteria is the same as the last
				// one, just return the collection.


				$criteria->add(UserFolderPeer::FOLDER_ID, $this->id);

				UserFolderPeer::addSelectColumns($criteria);
				if (!isset($this->lastUserFolderCriteria) || !$this->lastUserFolderCriteria->equals($criteria)) {
					$this->collUserFolders = UserFolderPeer::doSelect($criteria, $con);
				}
			}
		}
		$this->lastUserFolderCriteria = $criteria;
		return $this->collUserFolders;
	}

	/**
	 * Returns the number of related UserFolder objects.
	 *
	 * @param      Criteria $criteria
	 * @param      boolean $distinct
	 * @param      PropelPDO $con
	 * @return     int Count of related UserFolder objects.
	 * @throws     PropelException
	 */
	public function countUserFolders(Criteria $criteria = null, $distinct = false, PropelPDO $con = null)
	{
		if ($criteria === null) {
			$criteria = new Criteria(FolderPeer::DATABASE_NAME);
		} else {
			$criteria = clone $criteria;
		}

		if ($distinct) {
			$criteria->setDistinct();
		}

		$count = null;

		if ($this->collUserFolders === null) {
			if ($this->isNew()) {
				$count = 0;
			} else {

				$criteria->add(UserFolderPeer::FOLDER_ID, $this->id);

				$count = UserFolderPeer::doCount($criteria, false, $con);
			}
		} else {
			// criteria has no effect for a new object
			if (!$this->isNew()) {
				// the following code is to determine if a new query is
				// called for.  If the criteria is the same as the last
				// one, just return count of the collection.


				$criteria->add(UserFolderPeer::FOLDER_ID, $this->id);

				if (!isset($this->lastUserFolderCriteria) || !$this->lastUserFolderCriteria->equals($criteria)) {
					$count = UserFolderPeer::doCount($criteria, false, $con);
				} else {
					$count = count($this->collUserFolders);
				}
			} else {
				$count = count($this->collUserFolders);
			}
		}
		return $count;
	}

	/**
	 * Method called to associate a UserFolder object to this object
	 * through the UserFolder foreign key attribute.
	 *
	 * @param      UserFolder $l UserFolder
	 * @return     void
	 * @throws     PropelException
	 */
	public function addUserFolder(UserFolder $l)
	{
		if ($this->collUserFolders === null) {
			$this->initUserFolders();
		}
		if (!in_array($l, $this->collUserFolders, true)) { // only add it if the **same** object is not already associated
			array_push($this->collUserFolders, $l);
			$l->setFolder($this);
		}
	}


	/**
	 * If this collection has already been initialized with
	 * an identical criteria, it returns the collection.
	 * Otherwise if this Folder is new, it will return
	 * an empty collection; or if this Folder has previously
	 * been saved, it will retrieve related UserFolders from storage.
	 *
	 * This method is protected by default in order to keep the public
	 * api reasonable.  You can provide public methods for those you
	 * actually need in Folder.
	 */
	public function getUserFoldersJoinUser($criteria = null, $con = null, $join_behavior = Criteria::LEFT_JOIN)
	{
		if ($criteria === null) {
			$criteria = new Criteria(FolderPeer::DATABASE_NAME);
		}
		elseif ($criteria instanceof Criteria)
		{
			$criteria = clone $criteria;
		}

		if ($this->collUserFolders === null) {
			if ($this->isNew()) {
				$this->collUserFolders = array();
			} else {

				$criteria->add(UserFolderPeer::FOLDER_ID, $this->id);

				$this->collUserFolders = UserFolderPeer::doSelectJoinUser($criteria, $con, $join_behavior);
			}
		} else {
			// the following code is to determine if a new query is
			// called for.  If the criteria is the same as the last
			// one, just return the collection.

			$criteria->add(UserFolderPeer::FOLDER_ID, $this->id);

			if (!isset($this->lastUserFolderCriteria) || !$this->lastUserFolderCriteria->equals($criteria)) {
				$this->collUserFolders = UserFolderPeer::doSelectJoinUser($criteria, $con, $join_behavior);
			}
		}
		$this->lastUserFolderCriteria = $criteria;

		return $this->collUserFolders;
	}

	/**
	 * Clears out the collUnitFolders collection (array).
	 *
	 * This does not modify the database; however, it will remove any associated objects, causing
	 * them to be refetched by subsequent calls to accessor method.
	 *
	 * @return     void
	 * @see        addUnitFolders()
	 */
	public function clearUnitFolders()
	{
		$this->collUnitFolders = null; // important to set this to NULL since that means it is uninitialized
	}

	/**
	 * Initializes the collUnitFolders collection (array).
	 *
	 * By default this just sets the collUnitFolders collection to an empty array (like clearcollUnitFolders());
	 * however, you may wish to override this method in your stub class to provide setting appropriate
	 * to your application -- for example, setting the initial array to the values stored in database.
	 *
	 * @return     void
	 */
	public function initUnitFolders()
	{
		$this->collUnitFolders = array();
	}

	/**
	 * Gets an array of UnitFolder objects which contain a foreign key that references this object.
	 *
	 * If this collection has already been initialized with an identical Criteria, it returns the collection.
	 * Otherwise if this Folder has previously been saved, it will retrieve
	 * related UnitFolders from storage. If this Folder is new, it will return
	 * an empty collection or the current collection, the criteria is ignored on a new object.
	 *
	 * @param      PropelPDO $con
	 * @param      Criteria $criteria
	 * @return     array UnitFolder[]
	 * @throws     PropelException
	 */
	public function getUnitFolders($criteria = null, PropelPDO $con = null)
	{
		if ($criteria === null) {
			$criteria = new Criteria(FolderPeer::DATABASE_NAME);
		}
		elseif ($criteria instanceof Criteria)
		{
			$criteria = clone $criteria;
		}

		if ($this->collUnitFolders === null) {
			if ($this->isNew()) {
			   $this->collUnitFolders = array();
			} else {

				$criteria->add(UnitFolderPeer::FOLDER_ID, $this->id);

				UnitFolderPeer::addSelectColumns($criteria);
				$this->collUnitFolders = UnitFolderPeer::doSelect($criteria, $con);
			}
		} else {
			// criteria has no effect for a new object
			if (!$this->isNew()) {
				// the following code is to determine if a new query is
				// called for.  If the criteria is the same as the last
				// one, just return the collection.


				$criteria->add(UnitFolderPeer::FOLDER_ID, $this->id);

				UnitFolderPeer::addSelectColumns($criteria);
				if (!isset($this->lastUnitFolderCriteria) || !$this->lastUnitFolderCriteria->equals($criteria)) {
					$this->collUnitFolders = UnitFolderPeer::doSelect($criteria, $con);
				}
			}
		}
		$this->lastUnitFolderCriteria = $criteria;
		return $this->collUnitFolders;
	}

	/**
	 * Returns the number of related UnitFolder objects.
	 *
	 * @param      Criteria $criteria
	 * @param      boolean $distinct
	 * @param      PropelPDO $con
	 * @return     int Count of related UnitFolder objects.
	 * @throws     PropelException
	 */
	public function countUnitFolders(Criteria $criteria = null, $distinct = false, PropelPDO $con = null)
	{
		if ($criteria === null) {
			$criteria = new Criteria(FolderPeer::DATABASE_NAME);
		} else {
			$criteria = clone $criteria;
		}

		if ($distinct) {
			$criteria->setDistinct();
		}

		$count = null;

		if ($this->collUnitFolders === null) {
			if ($this->isNew()) {
				$count = 0;
			} else {

				$criteria->add(UnitFolderPeer::FOLDER_ID, $this->id);

				$count = UnitFolderPeer::doCount($criteria, false, $con);
			}
		} else {
			// criteria has no effect for a new object
			if (!$this->isNew()) {
				// the following code is to determine if a new query is
				// called for.  If the criteria is the same as the last
				// one, just return count of the collection.


				$criteria->add(UnitFolderPeer::FOLDER_ID, $this->id);

				if (!isset($this->lastUnitFolderCriteria) || !$this->lastUnitFolderCriteria->equals($criteria)) {
					$count = UnitFolderPeer::doCount($criteria, false, $con);
				} else {
					$count = count($this->collUnitFolders);
				}
			} else {
				$count = count($this->collUnitFolders);
			}
		}
		return $count;
	}

	/**
	 * Method called to associate a UnitFolder object to this object
	 * through the UnitFolder foreign key attribute.
	 *
	 * @param      UnitFolder $l UnitFolder
	 * @return     void
	 * @throws     PropelException
	 */
	public function addUnitFolder(UnitFolder $l)
	{
		if ($this->collUnitFolders === null) {
			$this->initUnitFolders();
		}
		if (!in_array($l, $this->collUnitFolders, true)) { // only add it if the **same** object is not already associated
			array_push($this->collUnitFolders, $l);
			$l->setFolder($this);
		}
	}


	/**
	 * If this collection has already been initialized with
	 * an identical criteria, it returns the collection.
	 * Otherwise if this Folder is new, it will return
	 * an empty collection; or if this Folder has previously
	 * been saved, it will retrieve related UnitFolders from storage.
	 *
	 * This method is protected by default in order to keep the public
	 * api reasonable.  You can provide public methods for those you
	 * actually need in Folder.
	 */
	public function getUnitFoldersJoinUnit($criteria = null, $con = null, $join_behavior = Criteria::LEFT_JOIN)
	{
		if ($criteria === null) {
			$criteria = new Criteria(FolderPeer::DATABASE_NAME);
		}
		elseif ($criteria instanceof Criteria)
		{
			$criteria = clone $criteria;
		}

		if ($this->collUnitFolders === null) {
			if ($this->isNew()) {
				$this->collUnitFolders = array();
			} else {

				$criteria->add(UnitFolderPeer::FOLDER_ID, $this->id);

				$this->collUnitFolders = UnitFolderPeer::doSelectJoinUnit($criteria, $con, $join_behavior);
			}
		} else {
			// the following code is to determine if a new query is
			// called for.  If the criteria is the same as the last
			// one, just return the collection.

			$criteria->add(UnitFolderPeer::FOLDER_ID, $this->id);

			if (!isset($this->lastUnitFolderCriteria) || !$this->lastUnitFolderCriteria->equals($criteria)) {
				$this->collUnitFolders = UnitFolderPeer::doSelectJoinUnit($criteria, $con, $join_behavior);
			}
		}
		$this->lastUnitFolderCriteria = $criteria;

		return $this->collUnitFolders;
	}

	/**
	 * Resets all collections of referencing foreign keys.
	 *
	 * This method is a user-space workaround for PHP's inability to garbage collect objects
	 * with circular references.  This is currently necessary when using Propel in certain
	 * daemon or large-volumne/high-memory operations.
	 *
	 * @param      boolean $deep Whether to also clear the references on all associated objects.
	 */
	public function clearAllReferences($deep = false)
	{
		if ($deep) {
			if ($this->collFiles) {
				foreach ((array) $this->collFiles as $o) {
					$o->clearAllReferences($deep);
				}
			}
			if ($this->collFileTmps) {
				foreach ((array) $this->collFileTmps as $o) {
					$o->clearAllReferences($deep);
				}
			}
			if ($this->collFoldersRelatedBySubfolderId) {
				foreach ((array) $this->collFoldersRelatedBySubfolderId as $o) {
					$o->clearAllReferences($deep);
				}
			}
			if ($this->collUserFolders) {
				foreach ((array) $this->collUserFolders as $o) {
					$o->clearAllReferences($deep);
				}
			}
			if ($this->collUnitFolders) {
				foreach ((array) $this->collUnitFolders as $o) {
					$o->clearAllReferences($deep);
				}
			}
		} // if ($deep)

		$this->collFiles = null;
		$this->collFileTmps = null;
		$this->collFoldersRelatedBySubfolderId = null;
		$this->collUserFolders = null;
		$this->collUnitFolders = null;
			$this->aUser = null;
			$this->aGroupe = null;
			$this->aFolderRelatedBySubfolderId = null;
			$this->aLicence = null;
			$this->aUsageDistribution = null;
			$this->aUsageConstraint = null;
			$this->aUsageUse = null;
			$this->aUsageCommercial = null;
			$this->aCreativeCommons = null;
			$this->aDisk = null;
	}

	// symfony_behaviors behavior
	
	/**
	 * Calls methods defined via {@link sfMixer}.
	 */
	public function __call($method, $arguments)
	{
	  if (!$callable = sfMixer::getCallable('BaseFolder:'.$method))
	  {
	    throw new sfException(sprintf('Call to undefined method BaseFolder::%s', $method));
	  }
	
	  array_unshift($arguments, $this);
	
	  return call_user_func_array($callable, $arguments);
	}

} // BaseFolder
